# PAUL Handoff

**Date:** 2026-05-29
**Status:** paused (APPLY 01-01 blocked on environment — Docker not yet available)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** BountyStrike-AIv7 — Claude Code-native autonomous bug bounty platform (recon → scan → validate → report).
**Core value:** Automated bug bounty hunting that produces only verified, non-duplicate findings.

---

## Current State

**Version:** v0.1 Community Edition MVP
**Phase:** 1 of 6 — Community Edition MVP: The Moat
**Plan:** 01-01 (DB foundation: PG17 custom image + pgvector + vectorscale + pg_search) — APPLY started, blocked

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ◐        ○     [APPLY in progress, blocked on Docker]
```

---

## What Was Done This Session

- Started `/paul:apply 01-01`. Validated approval (user invoked apply; STATE next-action matched).
- Loaded plan: 4 auto tasks, no checkpoints. files_modified:
  `Dockerfile.postgres-bs`, `docker-compose.yml`, `00_extensions.sql`,
  `10_phase1_vectorscale_indexes.sql`, `11_phase1_paradedb_search.sql`, `database-patterns.md`.
- Surveyed file state (pre-task): Dockerfile.postgres-bs MISSING, 10_/11_ SQL MISSING (to be created);
  docker-compose.yml (623 ln), 00_extensions.sql (18 ln), 01_schema.sql (312 ln), database-patterns.md (63 ln) EXIST.
- **BLOCKER FOUND:** No container runtime in this WSL distro. `docker`/`docker-compose` resolve only to
  Docker Desktop Windows bins (`/mnt/c/Program Files/Docker/...`); daemon DOWN; no `/var/run/docker.sock`;
  no docker-desktop WSL mount; podman/nerdctl absent.
- **No files written** — every empirical AC (AC-1/2/3/4/5/8) needs `docker build`/`compose`/`exec`.
  Audit made AC-1 the empirical feasibility gate; writing an unbuildable Dockerfile = false-completion. Stopped.

---

## What's In Progress

- **Docker enablement, route A (chosen):** enable Docker Desktop → Settings → Resources →
  WSL Integration → toggle this distro ON → Apply & Restart. (Route B = native engine install via
  get.docker.com was started but abandoned — `/tmp/get-docker.sh` downloaded, sudo needs password,
  user switched to A.)

---

## What's Next

**Immediate:** Confirm Docker reachable from WSL — `docker info` must succeed (daemon UP).
Then re-run `/paul:apply 01-01`.

**After Docker UP — execute 01-01's 4 tasks in order:**
1. CREATE `infra/docker/Dockerfile.postgres-bs` — PG17 base, pinned pgvector 0.8.0 / vectorscale 0.9.0 /
   pg_search v0.23.x + fixed base tag. Prefer glibc bookworm (NOT Alpine/musl — unverified). cargo-pgrx:
   match version + `pgrx init`. Verify: `docker build`.
2. EDIT `infra/docker/docker-compose.yml` — point postgres service at custom image.
3. CREATE `00_extensions.sql` activation + `10_phase1_vectorscale_indexes.sql` (DiskANN) +
   `11_phase1_paradedb_search.sql`. **BM25 must use `CREATE INDEX … USING bm25 … WITH (key_field='id')`,
   NOT deprecated `paradedb.create_bm25()`. Target verified-existing columns only — NOT findings.title/description
   (do not exist). First-boot init = plain CREATE INDEX, NO CONCURRENTLY.** Verify: compose up + functional
   `.so` smoke (not catalog-row check).
4. EDIT `docs/learnings/database-patterns.md` — document the build. Verify: grep + `uv run ruff check .`.

**ACs:** AC-1 build+load (empirical gate), AC-7 version pins, AC-8 idempotent re-apply, AC-2 compose uses image,
AC-3 three extensions functionally active, AC-4 migrations apply clean, AC-5 PG17 + schema preserved, AC-6 ruff clean.

**Then:** `/paul:unify 01-01`.

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/phases/01-community-edition-mvp/01-01-PLAN.md` | Plan being executed (676 ln) |
| `.paul/phases/01-community-edition-mvp/01-01-AUDIT.md` | Audit findings the plan now encodes |
| `infra/docker/docker-compose.yml` | postgres service to repoint (task 2) |
| `infra/sql/00_extensions.sql` | extension activation (task 3) |
| `infra/sql/01_schema.sql` | existing schema — verify column names before BM25 index |

---

## Resume Instructions

1. Verify Docker: `docker info` (must show daemon UP). If still down, finish route A toggle or fall back to route B.
2. Read `.paul/STATE.md` for latest position.
3. Re-run `/paul:apply 01-01` — start at Task 1 (nothing written yet, clean slate).

---

*Handoff created: 2026-05-29*
