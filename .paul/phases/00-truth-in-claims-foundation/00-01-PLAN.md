---
phase: 00-truth-in-claims-foundation
plan: 00-01
type: execute
wave: 1
depends_on: []
files_modified:
  # audit-added: actual set determined by Task 1 full-coverage grep across docs/ (excl. archive/).
  # Candidate set (22 files flagged by initial grep) — final list updated after Task 1:
  - docs/bountystrike_v6_phased_build_plan.md
  - docs/BountyStrike v6 Research Report Dual-Product Strategy, 2026 Pressure-Test & UX Blueprint.md
  - docs/research/02-routing-ev.md
  - docs/research/01-strategy-architecture.md
  - docs/research/03-verifier-antislop.md
  - docs/research/00c-context7-verifications.md
  - docs/claude-code-bug-bounty-build-plan.md
  - docs/codebase-summary.md
  - docs/project-overview-pdr.md
  - docs/architecture/bountystrike_v5_build_plan.md
  - (cost model file — TBD, located during Task 1)
autonomous: false
---

<objective>
## Goal
Correct the 6 empirical soft spots and the competitive-set errors across all primary docs, and rebuild the per-scan cost model with corrected DeepSeek pricing — so no load-bearing false claim propagates into code, cost models, or marketing.

## Purpose
v6 pressure-test found ~85% of claims verify, but 6 do not and 4 are load-bearing. Shipping any unfixed risks credibility the moment a sharp investor/partner/hire fact-checks. Gates benchmark publication in Phase 2.

## Output
All 6 claims corrected/reframed; competitive set fixed; cost model rebuilt; <$0.20/solo-scan target re-validated or re-set.
</objective>

<context>
@.paul/PROJECT.md
@docs/bountystrike_v6_phased_build_plan.md
@docs/research/02-routing-ev.md
@docs/research/03-verifier-antislop.md
</context>

<acceptance_criteria>
## AC-1: DeepSeek pricing corrected everywhere
Given the docs reference DeepSeek pricing
When grep for "0.14" / "0.28" pricing strings across docs/
Then all replaced with "$0.28 input / $0.42 output (cache-miss), $0.028 cache-hit input" and cost model rebuilt with explicit cache-hit ratio

## AC-2: Anthropic refusal claim reframed
Given the docs state a hard "70%+ refusal rate"
When grep for "70%" refusal references
Then the hard number is deleted and replaced with a mechanism description (runtime refusal classifier routing known-refusal payload categories to Venice Dolphin / Hermes-4-70B)

## AC-3: XBOW N/A rate labeled
Given the docs cite "~25% Informative/N/A"
When the claim is located
Then it is marked "industry estimate, not vendor-confirmed" OR dropped in favor of the qualitative argument

## AC-4: HackerOne structured_scopes verified
Given migration code depends on structured_scopes status
When the HackerOne API changelog is checked directly (15-min check)
Then status is recorded (deprecated true/false) in PROJECT.md/STATE.md and the Phase 1 W1.2 gate is marked resolved

## AC-5: Welch's t-test reframed
Given the docs present Welch's t-test as academically-derived
When the claim is located
Then it is presented as a calibrated engineering heuristic with measured FP/FN rates (no academic citation claimed)

## AC-6: EV decay constants documented
Given lambda=0.00065 / mu=0.00963 appear without derivation
When located in docs/research/02-routing-ev.md
Then documented as either regression-fit (with procedure + dataset) or hand-tuned (say so, mark "pending calibration")

## AC-7: Competitive set corrected
Given Surf AI is in the competitive analysis and Bugcrowd is under-ranked
When the competitive section is located
Then Surf AI is removed (with one-line rationale) and Bugcrowd (post-Mayhem, Nov 4 2025) is re-ranked as the most credible incumbent threat

<!-- audit-added: AC-8 (must-have #2, G1 provenance) -->
## AC-8: Every correction carries inline provenance
Given each empirical claim is corrected
When the edited line is reviewed
Then the new value carries an inline source-of-truth citation (parenthetical or footnote) so a fact-checker can trace it (e.g. "(DeepSeek official API pricing, verified 2026-05-27)")

<!-- audit-added: AC-9 (must-have #1, G2 full coverage) -->
## AC-9: Full doc coverage, no partial correction
Given the same stale claim may appear across many files
When Task 1 greps all of docs/ (excluding archive/)
Then every occurrence is in the edit map and corrected — partial correction (some files stale, some fixed) is a FAIL because inconsistent docs are worse than uncorrected ones

<!-- audit-added: AC-10 (must-have #3, G5 cost-overrun stop) -->
## AC-10: Cost-overrun stops, does not silently re-set
Given the rebuilt cost model produces a per-scan figure
When the figure exceeds $0.20
Then execution STOPS and the overrun is logged as a blocker in STATE.md — the target is NOT silently re-set without explicit operator decision
</acceptance_criteria>

<tasks>
<task type="checkpoint:human-action">
  <name>Verify HackerOne structured_scopes against changelog</name>
  <files>(external — HackerOne API changelog)</files>
  <action>Operator does the 15-min check against the HackerOne API changelog. Confirm whether structured_scopes is deprecated. Report result back.</action>
  <verify>Status recorded as true/false with source link</verify>
  <done>AC-4 satisfied: changelog status confirmed and recorded</done>
</task>

<!-- audit-added: pre-edit checkpoint (strongly-recommended #5, G3 rollback) -->
<task type="auto">
  <name>Confirm clean git state before bulk edits</name>
  <files>(repo root)</files>
  <action>Run git status. Confirm a clean checkpoint exists (no unrelated uncommitted edits mixed into the claim corrections). If the tree is dirty with unrelated work, surface it before editing so the correction diff stays reviewable/revertible.</action>
  <verify>git status reviewed; correction edits will land as an isolated, revertible diff</verify>
  <done>Clean revert point established</done>
</task>

<task type="auto">
  <name>Locate every empirical claim + cost model file (FULL coverage)</name>
  <files>docs/** (excluding docs/archive/**)</files>
  <action>Grep all 6 claim strings (0.14/0.28, 70% refusal, XBOW 25% N/A, Welch, 0.00065/0.00963, structured_scopes) and Surf AI / Bugcrowd across ALL of docs/ except archive/. Locate the cost model file (likely docs/research/02-routing-ev.md or a scripts/ cost calculator). Produce a complete file:line map of every edit site. Update frontmatter files_modified to the actual full set.</action>
  <verify>Map covers EVERY occurrence in docs/ (excl archive/) — not just the 6 candidate files; cost model located</verify>
  <done>AC-9 coverage established; no occurrence unaccounted for</done>
</task>

<task type="auto">
  <name>Apply empirical corrections (AC-1,2,3,5,6)</name>
  <files>docs/bountystrike_v6_phased_build_plan.md, docs/research/02-routing-ev.md, docs/research/03-verifier-antislop.md, and other sites from the map</files>
  <action>Edit each located claim across ALL sites from the Task 2 map: DeepSeek pricing → corrected figures; 70% refusal → mechanism description; XBOW 25% → labeled estimate or dropped; Welch's → calibrated heuristic framing; EV constants → derivation note. Preserve surrounding prose. <!-- audit-added: attach an inline source-of-truth citation to each corrected value (AC-8). --></action>
  <verify>Re-grep old wording → zero stale instances across all sites; AND grep new wording → present + identical at every site (catch single-site typos)</verify>
  <done>AC-1, AC-2, AC-3, AC-5, AC-6, AC-8 satisfied</done>
</task>

<task type="auto">
  <name>Rebuild cost model with cache-hit ratio (AC-1)</name>
  <files>(cost model file from Task 1)</files>
  <action>Rebuild per-scan cost calc using $0.28 input / $0.42 output cache-miss, $0.028 cache-hit input, with an explicit cache-hit ratio variable. Compute the new per-scan figure. <!-- audit-added: if figure > $0.20, STOP and log blocker in STATE.md — do NOT silently re-set the target (AC-10). --></action>
  <verify>Cost model produces a figure; if >$0.20 → blocker logged, execution halted for operator decision</verify>
  <done>AC-1 cost-model half satisfied; AC-10 enforced</done>
</task>

<task type="auto">
  <name>Correct competitive set (AC-7)</name>
  <files>docs/BountyStrike v6 Research Report... .md, docs/research/01-strategy-architecture.md</files>
  <action>Remove Surf AI from competitive analysis with one-line rationale (agentic security operations, not offensive-security competitor). Re-rank Bugcrowd post-Mayhem (Nov 4 2025) as most credible incumbent threat; note Mythos pricing ceiling ($25/$125 per MTok) risk.</action>
  <verify>Grep "Surf AI" → only the removal rationale remains; Bugcrowd re-ranked</verify>
  <done>AC-7 satisfied</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Code under control-plane/, mcp/, scripts/ — this plan is docs + cost-model only
- docs/archive/** — historical, leave frozen
- The monorepo split / license headers — that is plan 00-02, not this one

## SCOPE LIMITS
- Edit only the claims enumerated in the Task 1 map
- No new sections, no rewrites beyond the 6 claims + competitive set
- Cost model: rebuild the calculation, do not redesign the routing architecture
</boundaries>

<verification>
- [ ] Re-grep all 6 claim strings → no stale wording remains across ALL of docs/ (excl archive/)
- [ ] New wording present AND consistent at every edit site (no single-site typo) <!-- audit-added -->
- [ ] Every corrected claim carries an inline source-of-truth citation <!-- audit-added -->
- [ ] "Surf AI" appears only in removal rationale
- [ ] Bugcrowd re-ranked as top incumbent threat
- [ ] Cost model emits a number; if >$0.20 → blocker logged (not silently re-set) <!-- audit-added -->
- [ ] HackerOne structured_scopes status recorded in STATE.md deferred-issues → resolved
</verification>

<success_criteria>
- All 6 empirical claims corrected or reframed in every primary doc
- HackerOne structured_scopes status confirmed (true/false) and Phase 1 W1.2 gate resolved
- Cost model rebuilt with corrected DeepSeek pricing and cache-hit ratio
- Competitive set fixed (Surf AI out, Bugcrowd re-ranked)
</success_criteria>

<output>
After completion, create .paul/phases/00-truth-in-claims-foundation/00-01-SUMMARY.md
</output>
