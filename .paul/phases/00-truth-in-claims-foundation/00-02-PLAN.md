---
phase: 00-truth-in-claims-foundation
plan: 00-02
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - turbo.json
  - package.json
  - LICENSE
  - LICENSE-APACHE
  - LICENSE-AGPL
  - LICENSES.md
  - CONTRIBUTING.md
  - CLA.md
  - packages/core/LICENSE
  - packages/community/LICENSE
  - packages/enterprise/LICENSE
  - .gitignore
autonomous: true
---

<objective>
## Goal
Add the license/compliance layer and a pnpm + Turborepo task-orchestration layer ON TOP of the existing uv Python workspace — without moving or breaking existing code.

## Purpose
v6 Phase 0 Workstream 0.3 requires the three-root license split (Apache-2.0 core / AGPL-3.0 community / proprietary enterprise) and a monorepo task layer. The repo is already a uv workspace (control-plane + 15 MCPs); operator decision is HYBRID — keep uv, layer Turborepo on top, map existing dirs to license roots via marker dirs + LICENSE files rather than a physical move (the physical reorg is deferred). This establishes the legal posture every later phase depends on.

## Output
pnpm-workspace.yaml + turbo.json + root package.json; three license root markers with LICENSE files; root LICENSE manifest; CONTRIBUTING.md + CLA.md.
</objective>

<context>
@.paul/PROJECT.md
@.paul/STATE.md
@pyproject.toml
@docs/bountystrike_v6_phase0-1_technical_brief.md
</context>

<acceptance_criteria>
## AC-1: pnpm workspace + Turborepo task layer present, uv untouched
Given the repo is a uv Python workspace
When pnpm-workspace.yaml, turbo.json, and root package.json are added
Then `pyproject.toml` [tool.uv.workspace] members are UNCHANGED, and turbo.json uses Turborepo 2.x `tasks:` syntax (NOT `pipeline:`)

## AC-2: Three license roots with correct licenses
Given the three-root split (core=Apache-2.0, community=AGPL-3.0, enterprise=proprietary)
When packages/{core,community,enterprise}/ marker dirs + LICENSE files exist
Then packages/core/LICENSE = Apache-2.0 full text, packages/community/LICENSE = AGPL-3.0 full text, packages/enterprise/LICENSE = proprietary notice

## AC-3: Root LICENSE manifest points to per-root licenses
Given mixed licensing
When root LICENSE is read
Then it is a manifest explaining the three-root split and pointing at each per-root LICENSE (not a single blanket license)

## AC-4: Contribution governance present
Given an AGPLv3 OSS project needs contribution terms
When CONTRIBUTING.md and CLA.md exist
Then CONTRIBUTING.md states the license boundaries (no community→core imports) and CLA.md states contributor license assignment terms

## AC-5: turbo tasks runnable
Given turbo.json defines tasks
When `pnpm turbo run lint --dry` (or `npx turbo run lint --dry`) is invoked
Then turbo parses the config without error and lists the task graph

<!-- audit-added: AC-6 (must-have #1, G1 license-text integrity) -->
## AC-6: License texts are verbatim canonical, not paraphrased
Given an LLM can paraphrase license text (legally invalid)
When packages/core/LICENSE and packages/community/LICENSE are checked
Then they contain the EXACT canonical SPDX text — Apache-2.0 must include its verbatim "TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION" through the appendix; AGPL-3.0 must include its verbatim preamble + all sections 0–17 including §13 "Remote Network Interaction" — sourced from spdx.org/licenses, never generated from memory

<!-- audit-added: AC-7 (must-have #2, G2 license↔code binding) -->
## AC-7: Existing code dirs explicitly bound to tier + license
Given the hybrid keeps code at control-plane/ and mcp/ (not under packages/community/)
When LICENSES.md (root) is read
Then it is a mapping table binding every existing top-level code dir (control-plane/, each mcp/*, scripts/) to its tier (community) and license (AGPL-3.0), so the license↔code binding is explicit and machine-checkable — not asserted only in prose

<!-- audit-added: AC-8 (must-have #3, G3 AGPL §13 + relicensing boundary) -->
## AC-8: AGPL §13 network-use obligation + relicensing boundary stated
Given the dual-product strategy is AGPL community + proprietary SaaS
When CONTRIBUTING.md and root LICENSE are read
Then both state (a) the AGPL-3.0 §13 network-use / source-offer obligation that attaches to community-tier code, and (b) the core(Apache-2.0) → community(AGPL-3.0) → enterprise(proprietary) relicensing boundary, so the dual-product license posture is unambiguous
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Add pnpm + Turborepo task layer (uv untouched)</name>
  <files>pnpm-workspace.yaml, turbo.json, package.json, .gitignore</files>
  <action>
Create pnpm-workspace.yaml with packages globs: "packages/core/*", "packages/community/*", "packages/enterprise/*", "apps/*".
Create root package.json: name "bountystrike", private true, packageManager "pnpm@9", devDependency "turbo@^2.9.0", scripts {build,test,lint,dev} delegating to turbo.
Create turbo.json with $schema, and "tasks:" (NOT "pipeline:" — Turborepo 2.x) defining build (dependsOn ["^build"], outputs dist/**), test (dependsOn ["build"]), lint ({}), dev (cache false, persistent true).
Append node_modules/, .turbo/ to .gitignore if not present.
Do NOT touch pyproject.toml or [tool.uv.workspace].
  </action>
  <verify>cat turbo.json shows "tasks" not "pipeline"; pyproject.toml unchanged (git diff pyproject.toml empty)</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Create three license roots + LICENSE files</name>
  <files>packages/core/LICENSE, packages/community/LICENSE, packages/enterprise/LICENSE, LICENSE-APACHE, LICENSE-AGPL</files>
  <action>
Create marker dirs packages/core/, packages/community/, packages/enterprise/ each with a .gitkeep and a README.md one-liner stating the license + intended contents (core=shared libs/schemas Apache-2.0; community=9 subagents/CLI/self-host AGPL-3.0; enterprise=multi-tenant/SSO/AEV proprietary).
Fetch VERBATIM canonical Apache-2.0 text from spdx.org/licenses/Apache-2.0.txt (or pinned canonical mirror) → packages/core/LICENSE (and copy at root LICENSE-APACHE). Do NOT generate from memory.
Fetch VERBATIM canonical AGPL-3.0 text from gnu.org/licenses/agpl-3.0.txt (or spdx.org) → packages/community/LICENSE (and copy at root LICENSE-AGPL). MUST include §13 "Remote Network Interaction". Do NOT generate from memory.
Write proprietary notice → packages/enterprise/LICENSE ("All rights reserved. Proprietary — not licensed for redistribution. Contact BountyStrike for commercial terms.").
<!-- audit-added (AC-6): license texts MUST be byte-faithful canonical SPDX/FSF text via WebFetch/curl, never paraphrased. -->
  </action>
  <verify>head packages/core/LICENSE shows "Apache License" + "Version 2.0"; packages/community/LICENSE contains BOTH "GNU AFFERO GENERAL PUBLIC LICENSE" AND "13. Remote Network Interaction" (proves full canonical text incl §13); packages/enterprise/LICENSE shows proprietary notice</verify>
  <done>AC-2, AC-6 satisfied</done>
</task>

<task type="auto">
  <name>Root LICENSE manifest + LICENSES.md mapping + governance docs</name>
  <files>LICENSE, LICENSES.md, CONTRIBUTING.md, CLA.md</files>
  <action>
Write root LICENSE as a MANIFEST (not a blanket license): explain the three-root split, point at packages/core/LICENSE (Apache-2.0), packages/community/LICENSE (AGPL-3.0), packages/enterprise/LICENSE (proprietary). State the AGPL-3.0 §13 network-use/source-offer obligation on community-tier code, and the core(Apache) → community(AGPL) → enterprise(proprietary) relicensing boundary.
<!-- audit-added (AC-7): create LICENSES.md as an explicit mapping table -->
Write LICENSES.md: a table binding EVERY existing top-level code dir to its tier + license — control-plane/ → community/AGPL-3.0; each mcp/* → community/AGPL-3.0; scripts/ → community/AGPL-3.0; packages/core/* → Apache-2.0; packages/enterprise/* → proprietary. This is the explicit license↔code binding (the hybrid keeps code outside packages/, so the mapping must be machine-checkable here, not prose-only).
Write CONTRIBUTING.md: dev setup (uv sync, pnpm install), the hard rule "no imports from community→core" (core stays Apache-clean), the AGPL-3.0 §13 network-use obligation, license-header expectation per tier, PR process.
<!-- audit-added (G4): CLA aspirational note -->
Write CLA.md: contributor license terms (contributor grants project rights to relicense within the three-tier model). State clearly it is ASPIRATIONAL until a DCO/sign-off enforcement mechanism is wired (a later plan) — do not imply automated enforcement exists.
  </action>
  <verify>LICENSE references all three per-root licenses AND states AGPL §13 + relicensing boundary; LICENSES.md maps control-plane/ + mcp/* + scripts/ to AGPL-3.0; CONTRIBUTING.md states community→core import prohibition AND §13; CLA.md present with aspirational note</verify>
  <done>AC-3, AC-4, AC-7, AC-8 satisfied</done>
</task>

<task type="auto">
  <name>Verify turbo parses</name>
  <files>(none — verification only)</files>
  <action>Run `npx turbo run lint --dry=json` (or `pnpm dlx turbo@^2.9.0 run lint --dry`) to confirm turbo.json parses and produces a task graph. If pnpm/node absent, document as environment gap (do not fail the plan — config correctness is the deliverable, not a live run).</action>
  <verify>turbo --dry exits 0 and emits a task plan, OR environment gap documented with the config validated by schema</verify>
  <done>AC-5 satisfied (or documented env gap)</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- pyproject.toml / [tool.uv.workspace] — the uv workspace stays authoritative for Python
- control-plane/, mcp/, scripts/, tests/ — no physical moves in this plan (deferred)
- The 7 docs corrected in 00-01 + the brief
- .paul/ internals

## SCOPE LIMITS
- License layer + pnpm/Turborepo task layer + governance docs ONLY
- NO physical relocation of existing code under packages/ (future plan)
- NO feature-flag SDK (that is 00-03)
- NO license-header injection into existing source files (future plan; CONTRIBUTING states the expectation)
</boundaries>

<verification>
- [ ] pyproject.toml [tool.uv.workspace] unchanged (git diff empty)
- [ ] turbo.json uses `tasks:` not `pipeline:` (Turborepo 2.x trap)
- [ ] Three per-root LICENSE files with VERBATIM canonical SPDX/FSF texts (AGPL incl §13) <!-- audit-added -->
- [ ] LICENSES.md binds control-plane/ + mcp/* + scripts/ to AGPL-3.0 (explicit license↔code map) <!-- audit-added -->
- [ ] Root LICENSE is a manifest pointing at all three + states §13 + relicensing boundary <!-- audit-added -->
- [ ] CONTRIBUTING.md states community→core import prohibition + AGPL §13 <!-- audit-added -->
- [ ] CLA.md present with aspirational/no-enforcement note <!-- audit-added -->
- [ ] turbo --dry parses (or env gap documented)
</verification>

<success_criteria>
- Three-root license split established without breaking the uv workspace
- Turborepo 2.x task layer parses
- Contribution governance (CONTRIBUTING + CLA) in place
- Phase 0 Workstream 0.3 license + repo-layout exit criteria partially met (headers + physical reorg deferred to later plans)
</success_criteria>

<output>
After completion, create .paul/phases/00-truth-in-claims-foundation/00-02-SUMMARY.md
</output>
