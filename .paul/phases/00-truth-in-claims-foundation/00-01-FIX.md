---
phase: 00-truth-in-claims-foundation
plan: 00-01-FIX
type: fix
wave: 1
depends_on: ["00-01"]
files_modified:
  - docs/architecture/bountystrike_v5_build_plan.md
  - docs/research/02-routing-ev.md
  - docs/research/01-strategy-architecture.md
  - docs/research/06-roadmap.md
  - docs/project-overview-pdr.md
  - docs/beyond bountystrike.md
  - docs/claude-code-bug-bounty-build-plan.md
autonomous: true
---

<objective>
## Goal
Re-correct the DeepSeek pricing that plan 00-01 set WRONG, using the primary-source figures, and fix the now-incorrect provenance citations 00-01 attached.

## Purpose
Plan 00-01 trusted the v6 plan's "Correction #1" ($0.28 in / $0.42 out) and propagated it into 7 docs with citations claiming "DeepSeek official API pricing, verified v6 2026-05-27". The Phase 0/1 technical brief (primary source: api-docs.deepseek.com/quick_start/pricing, May 21 2026) proves BOTH the v5 figure AND the v6 correction wrong. Correct figure: **$0.14 cache-miss input / $0.0028 cache-hit input / $0.28 output per 1M**. The citations we added now point at a wrong source — a credibility defect in the exact docs Phase 0 exists to make defensible.

## Output
All DeepSeek pricing = $0.14/$0.0028/$0.28; citations point at the brief (primary source); cost model rebuilt; SUMMARY written.

Source: docs/bountystrike_v6_phase0-1_technical_brief.md (SIX CORRECTIONS #1)
Priority: 1 major (wrong pricing in 7 docs + wrong citations + wrong cost model)
</objective>

<context>
@.paul/STATE.md
@docs/bountystrike_v6_phase0-1_technical_brief.md
@.paul/phases/00-truth-in-claims-foundation/00-01-PLAN.md
</context>

<acceptance_criteria>
## AC-1: DeepSeek pricing matches primary source everywhere
Given the docs were set to the wrong $0.28/$0.42 by plan 00-01
When grep for DeepSeek pricing across all 7 edited docs
Then every figure reads "$0.14 cache-miss input / $0.0028 cache-hit input / $0.28 output per 1M" (or table-cell equivalent "0.14 / 0.28 (cache-hit 0.0028)")

## AC-2: Provenance citations corrected
Given 00-01 added "(DeepSeek official API pricing, verified v6 2026-05-27)" pointing at the wrong v6 figure
When each pricing citation is reviewed
Then it points at the primary source: "(DeepSeek official API pricing per api-docs.deepseek.com, see docs/bountystrike_v6_phase0-1_technical_brief.md — corrects the v6 figure)"

## AC-3: Cost model rebuilt with correct numbers
Given research/02-routing-ev.md cost model used the wrong $0.28 input
When the triage line and total are recomputed with $0.14 input (cache-miss) and $0.0028 cache-hit
Then the figure is consistent and the per-scan cost is re-checked against the <$0.20 target (with the cache-hit lever even cheaper now)

## AC-4: No stale wrong figure remains
Given the wrong $0.28/$0.42 was applied to 7 docs
When grep "0.42" and "0.028" (the wrong cache-hit) across the 7 docs
Then zero instances remain outside the v6-doc correction record (which is not in this edit set)
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Fix DeepSeek pricing + citations in v5 build plan</name>
  <files>docs/architecture/bountystrike_v5_build_plan.md</files>
  <action>Find every DeepSeek pricing figure 00-01 set to "$0.28 / $0.42" / "$0.28 input / $0.42 output (cache-hit 0.028)" and replace with the primary-source figure: "$0.14 cache-miss input / $0.0028 cache-hit input / $0.28 output per MTok". Fix the recomputed cost-model prose (00-01 changed 500K×$0.28=$0.14; correct is 500K×$0.14=$0.07). Replace the citation "(DeepSeek official API pricing, verified v6 2026-05-27)" with "(per api-docs.deepseek.com; see docs/bountystrike_v6_phase0-1_technical_brief.md — corrects v6 figure)".</action>
  <verify>grep "0.42" docs/architecture/bountystrike_v5_build_plan.md returns no DeepSeek pricing hits; grep "0.0028" present</verify>
  <done>AC-1, AC-2 satisfied for this file</done>
</task>

<task type="auto">
  <name>Fix DeepSeek pricing + cost model in research docs + project-overview</name>
  <files>docs/research/02-routing-ev.md, docs/research/01-strategy-architecture.md, docs/research/06-roadmap.md, docs/project-overview-pdr.md</files>
  <action>Same pricing correction across these 4 files. In research/02 routing table cell: "0.28 / 0.42 (cache-hit 0.028)" → "0.14 / 0.28 (cache-hit 0.0028)". Rebuild the cost-model block 00-01 wrote: triage 500K × $0.14/M = $0.07 (not $0.14); total $0.20 + $0.07 + $0.00 + $0.15 + $0.09 = $0.51 (cache-miss); with cache-hit input $0.0028/M triage ≈ $0.0014, total ≈ $0.44. Keep the per-scan <$0.20 target check (still passes, even cheaper). Fix all citations to point at the brief.</action>
  <verify>grep "0.42" across the 4 files → no DeepSeek hits; cost model totals consistent</verify>
  <done>AC-1, AC-2, AC-3 satisfied for these files</done>
</task>

<task type="auto">
  <name>Fix DeepSeek pricing in beyond-bountystrike + claude-code build plan</name>
  <files>docs/beyond bountystrike.md, docs/claude-code-bug-bounty-build-plan.md</files>
  <action>beyond bountystrike.md pricing table: V4-Flash row 00-01 set to "0.28 (cache 0.028) | 0.42" → correct to "0.14 (cache 0.0028) | 0.28"; V3.2 row "0.28 | 0.42" → "0.14 | 0.28"; prose line ~338 fix. claude-code-bug-bounty-build-plan.md:219 prose "$0.28 input / $0.42 output per million (cache-hit input $0.028)" → "$0.14 input / $0.28 output per million (cache-hit input $0.0028)". Fix citations to point at the brief.</action>
  <verify>grep "0.42" both files → no DeepSeek hits</verify>
  <done>AC-1, AC-2, AC-4 satisfied for these files</done>
</task>

<task type="auto">
  <name>Final coverage grep</name>
  <files>docs/** (excluding archive/ and the two v6 docs)</files>
  <action>Grep the wrong figures "0.42" and "(cache 0.028)" / "$0.028" across docs/ excluding archive/ and the two v6 docs. Confirm zero DeepSeek-pricing instances remain. The two v6 docs legitimately retain $0.28/$0.42 references as the (now-known-wrong) correction record — out of scope.</action>
  <verify>Zero wrong-figure DeepSeek hits outside v6 docs + archive</verify>
  <done>AC-4 satisfied globally</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- The two v6 docs (bountystrike_v6_phased_build_plan.md, v6 Research Report) — they hold the v6 correction as a record; a separate note already flags it as superseded by the brief
- Any claim other than DeepSeek pricing — refusal/XBOW/Welch/EV/competitive-set corrections from 00-01 stand (the brief re-audited them as still valid)
- Code under control-plane/, mcp/, scripts/

## SCOPE LIMITS
- Only DeepSeek pricing + its citations + the cost model
- Do NOT re-open the other 6 corrections (brief confirms them valid)
</boundaries>

<verification>
- [ ] All DeepSeek pricing = $0.14/$0.0028/$0.28 across the 7 docs
- [ ] All pricing citations point at the brief (primary source), not the wrong v6 figure
- [ ] Cost model recomputed and consistent; per-scan still < $0.20
- [ ] grep "0.42" → no DeepSeek hits outside v6 docs
</verification>

<success_criteria>
- DeepSeek pricing corrected to primary source in all 7 docs
- Provenance now traceable to api-docs.deepseek.com via the brief
- Ready for re-verification
</success_criteria>

<output>
After completion, create .paul/phases/00-truth-in-claims-foundation/00-01-FIX-SUMMARY.md
</output>
