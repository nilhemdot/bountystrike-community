# PAUL Session Handoff

**Session:** 2026-05-29
**Phase:** 1 — Community Edition MVP: The Moat
**Context:** 01-01 DB foundation loop closed; next is planning 01-02.

---

## Session Accomplishments

- **APPLY 01-01** — built custom `bs-postgres:pg17` image (PG 17.10 + pgvector 0.8.0 + vectorscale 0.9.0/pgrx 0.16.1 + pg_search 0.23.5, glibc bookworm).
- All 8 ACs + G8 functional smoke verified live (3 extensions active; DiskANN + BM25 indexes apply + idempotent on re-apply; 5 tables intact).
- **UNIFY 01-01** — SUMMARY authored, loop closed (PLAN ✓ APPLY ✓ UNIFY ✓), STATE.md + paul.json updated, 3 deviations recorded.
- Codebase indexed (claude-context: 193 files, 1298 chunks).

## Files created/modified

**New:** `infra/docker/Dockerfile.postgres-bs`, `infra/sql/10_phase1_vectorscale_indexes.sql`, `infra/sql/11_phase1_paradedb_search.sql`, `.paul/phases/01-community-edition-mvp/01-01-SUMMARY.md`, throwaway `infra/docker/.env` (gitignored).
**Modified:** `infra/docker/docker-compose.yml` (+`-c shared_preload_libraries=pg_search`), `infra/sql/00_extensions.sql`, `docs/learnings/database-patterns.md`, `.paul/STATE.md`, `.paul/paul.json`.

---

## Decisions Made

| Decision | Rationale | Impact |
|----------|-----------|--------|
| glibc `postgres:17-bookworm` base, not Alpine/musl | pgrx + ParadeDB are glibc-only | Custom PG ext images use glibc base going forward |
| Added `shared_preload_libraries=pg_search` to compose command | BM25 index AM + bg worker require preload at server start (Context7) | Hard runtime requirement for the image |
| Pre-existing ruff debt out of 01-01 scope | 01-01 touched zero `.py`; AC-6 "ruff 0" unsatisfiable without violating no-domain boundary | Recorded as deviation; needs separate triage |
| 01-02 = Hatchet v1 + evidence store as a split candidate | Two subsystems; PAUL single-concern sizing | May split into 01-02 Hatchet / 01-03 evidence (renumber downstream) |

---

## Gap Analysis with Decisions

### Pre-existing ruff debt (40 errors)
**Status:** DEFER
**Notes:** All in non-01-01 `.py` (control-plane/, mcp/, scripts/, tests/). Not a 01-01 regression. Triage in a dedicated pass, not inside a feature plan.
**Reference:** `@.paul/phases/01-community-edition-mvp/01-01-SUMMARY.md`

### Base-image RepoDigest not pinned
**Status:** DEFER
**Notes:** Image present without digest ref; server_version 17.10 recorded as the concrete pin proof. Hardening item: `FROM ...@sha256:<digest>` after `docker pull`.
**Reference:** `@infra/docker/Dockerfile.postgres-bs`

### research/02-routing-ev.md scope-migration date wrong
**Status:** DEFER
**Notes:** "April 16" should be April 7 2026; needs `scope_exclusions` endpoint added. Fix in a future Phase 1 plan.
**Reference:** STATE.md Deferred Issues

---

## Open Questions

- **01-02 scope split:** ship Hatchet + evidence as one plan, or split (01-02 Hatchet / 01-03 evidence)? Roadmap line 62 lists both under 01-02; PAUL sizing favors split.
- 01-01 changes are **uncommitted** — phase-transition commit fires only at full Phase 1 completion. Confirm that holds.

---

## Reference Files for Next Session

```
@.paul/phases/01-community-edition-mvp/01-01-SUMMARY.md
@.paul/STATE.md
@.paul/ROADMAP.md
@docs/learnings/database-patterns.md   # Phase 1 extension-traps incl. boto3/R2 note for 01-02
@CLAUDE.md                              # traps #2 Hatchet v1, #5 boto3<1.36, #1 claude-agent-sdk
```

---

## Prioritized Next Actions

| Priority | Action | Effort |
|----------|--------|--------|
| 1 | `/paul:plan 01-02` — Hatchet v1 task API (`@hatchet.task()`, `aio_` prefix, Pydantic inputs) + R2 evidence store (`aioboto3`, `boto3<1.36` or `request_checksum_calculation="when_required"`); confirm split decision first | M |
| 2 | Triage pre-existing ruff debt (40 errors) | S |
| 3 | Pin base-image digest in Dockerfile.postgres-bs | S |

---

## State Summary

**Current:** Phase 1, plan 01-01 loop CLOSED (PLAN ✓ APPLY ✓ UNIFY ✓). Phase 1 ~17% (1 of ~6 plans).
**Next:** `/paul:plan 01-02` — confirm scope split, then author PLAN.
**Resume:** `/paul:resume` then read this handoff.

---

*Handoff created: 2026-05-29*
