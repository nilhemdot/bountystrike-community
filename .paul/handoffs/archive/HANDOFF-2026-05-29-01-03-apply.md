# PAUL Handoff

**Date:** 2026-05-29
**Status:** paused (context-limit — paused BEFORE APPLY 01-03 to preserve headroom)

---

## READ THIS FIRST

No prior context needed. This doc + STATE.md tells you everything.

**Project:** BountyStrike-AIv7 — Claude Code-native autonomous bug bounty platform.
**Core value:** Only verified, non-duplicate findings (deterministic verifier is the moat).

---

## Current State

**Phase:** 1 of 6 — Community Edition MVP (The Moat). 2 of ~7 plans done.
**Plan:** 01-03 (Evidence store / R2) — PLAN ✓ + AUDIT ✓, APPLY not started.

**Loop:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○     [planned + audited, awaiting APPLY]
```

---

## What Was Done This Session

- Committed two closed loops: **4c84845** (01-01 DB foundation + 01-02 Hatchet v1, 22 files).
- Wrote **01-03-PLAN.md** (standard, 3 tasks): fix R2 boto3≥1.36 checksum trap, add atomic
  `EvidenceRecordingService` (blob→evidence_artifacts row→hash-chained audit), wire as durable
  Hatchet `record-evidence` task + orchestrator trigger.
- Ran enterprise **AUDIT** → 01-03-AUDIT.md. Applied 3 must-have + 3 strongly-rec; deferred 2.
  Verdict: conditionally acceptable.
- Fixed ROADMAP plan-list drift (renumbered to 7 plans; 01-01/01-02 marked done).

## What's In Progress

- **01-03 APPLY** — not started. Plan + audit on disk, uncommitted.

## What's Next

**Immediate:** fresh session → `/paul:apply .paul/phases/01-community-edition-mvp/01-03-PLAN.md`
**After:** `/paul:unify` 01-03, then plan 01-04 (scope ingestion + RS256 JWT).

---

## Critical APPLY Context (read before APPLY)

- **Most of evidence_management already EXISTS** in control-plane: `R2BlobStore` (aioboto3),
  `make_blob_store` factory, `HashChainService` (pg_advisory_xact_lock), `evidence_artifacts`
  + realigned `audit_log` tables (migration 03), aggregates, `scripts/r2_smoke.py`. APPLY only
  closes 3 gaps — DO NOT rebuild existing pieces.
- **Audit-encoded must-dos:** (M1) hash-chain genesis prev_hash = `b""` (0-byte), NOT 32 zero
  bytes — migration 03 + HashChainService convention. (M2) moto canNOT reproduce R2 CRC
  rejection → AC-1 must assert botocore Config values by introspection, not moto round-trip.
  (M3) idempotent re-record must append NO second audit entry (replay-fork risk).
- boto3 1.40.61 / aioboto3 15.5.0 → R2 client needs `request_checksum_calculation="when_required"`
  + `response_checksum_validation="when_required"` (CLAUDE.md trap #5).
- evidence-mcp (stdio, local-FS+SQLite) ↔ control-plane (R2+PG) divergence = OUT OF SCOPE, deferred.
- **HARNESS RISK:** 01-02 session hit tool-output corruption at high context. Run APPLY
  strict-sequential (one tool at a time), re-verify every claim against disk/sandbox.
- LIVE: bs-hatchet + bs-hatchet-postgres containers UP (engine left running from 01-02).
  Worker STOPPED. Hatchet client token session-ephemeral (regen via 01-02 SUMMARY runbook).

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live state |
| `.paul/phases/01-community-edition-mvp/01-03-PLAN.md` | Plan to APPLY |
| `.paul/phases/01-community-edition-mvp/01-03-AUDIT.md` | Audit findings |

## Resume Instructions

1. Read STATE.md (esp. 01-03 audit Decisions row + this handoff's APPLY context).
2. `/paul:apply .paul/phases/01-community-edition-mvp/01-03-PLAN.md` — strict-sequential.

---
*Handoff created: 2026-05-29*
