# Project State

## Project Reference

See: .paul/PROJECT.md (updated 2026-05-29)

**Core value:** Automated bug bounty hunting that produces only verified, non-duplicate findings
**Current focus:** Phase 2 — Full Agent Fleet, Anti-Slop & Benchmark (02-03 exploit-agent wiring — COMPLETE/loop closed; next /paul:plan 02-04. Phase 2 = 3/6)

## Current Position

Milestone: v0.1 Community Edition MVP
Phase: 2 of 6 (Full Agent Fleet, Anti-Slop & Benchmark) — IN PROGRESS. Phase 1 COMPLETE (10/10). v0.1 Community Edition MVP MILESTONE COMPLETE — released 2026-05-31 (commit 3709c08, tag v0.1.0 3bdf41f, PUBLIC nilhemdot/bountystrike-community). Phase 2: 2/6 plans complete (02-01, 02-02).
Plan: 02-03 PLAN CREATED (awaiting audit/approval) — exploit-agent wiring onto the 02-02 docker substrate. 02-02 COMPLETE (02-02-SUMMARY.md). 02-01 COMPLETE (02-01-SUMMARY.md). Phase 2 RE-SLICED 5→6 (AskUserQuestion-confirmed 2026-05-31): 02-02 = sandbox docker-driver substrate (DONE); 02-03 = exploit-agent wiring (THIS, PLAN CREATED); 02-04 = ai-vuln-hunter/cloud-recon/reporter; 02-05 = anti-slop+SLO+killswitch; 02-06 = XBOW benchmark+publish. Next action: /paul:audit 02-03 (audit gate ENABLED) → /paul:apply. Apply MUST re-verify contracts vs disk FIRST (planning-corruption seen in 02-01/01-02 sessions).

02-03 SHAPE: STANDARD, 3 auto tasks + 1 checkpoint:human-verify, autonomous=FALSE, AC-1..AC-6, depends_on=[02-02] (wires onto the docker substrate). DECISIONS (AskUserQuestion 2026-05-31): track=standard; verify-depth=FULL live exploit-agent dry-run; wiring-scope=FULL (enum + spec prose + result.status→verdict + MCP tool grants + orchestrator SANDBOX_DRIVER handoff); validator-spec=both — BUT recon CORRECTED the premise: validator-agent.md is ALREADY Firecracker-clean (grep=0 matches); the only stale "fresh microVM" line is INSIDE exploit-agent.md Step 8 → reconciled there, validator-agent.md is assert-clean only (no edit). RECON GROUND TRUTH (verified 2026-05-31, batch tools agree): (1) finding_status ENUM (01_schema:16-33; 06 ALTER-added exploit_pending_validation) MISSING exploit_failed_oos/timeout/crash — spec Step 6 writes all 3 → live UPDATE raises invalid-enum = THE blocker; next migration=13 (00-12 exist); reuse 06's `ALTER TYPE ... ADD VALUE IF NOT EXISTS 'x' AFTER 'y'` idempotent pattern (bare, non-txn init path). (2) exploit-agent.md Firecracker-worded throughout run path (desc, Inputs SANDBOX_MCP_URL, Step 6 "fresh Firecracker microVM", Step 8 microVM, Dependencies) → reconcile to docker substrate + ONE Phase-3(task214) deferral note. (3) API drift: spec reads result.status; tools return `verdict` (values map 1:1) → fix to result.verdict (KeyError otherwise). (4) MCP-grant drift: exploit-agent.md frontmatter tools=[Bash,Read,Write,WebFetch] ONLY — NO mcp__sandbox/evidence/dedup despite Safety Rule 3 + Steps 6-7 requiring them (trap #12 allowlist blocks) → add 6 mcp__* grants. (5) SANDBOX_DRIVER handoff gap: orchestrator _exploit_one passes SANDBOX_MCP_URL but NOT SANDBOX_DRIVER; .mcp.json sandbox block has NO env → stdio sandbox-mcp would raise "SANDBOX_DRIVER unset" → inject SANDBOX_DRIVER=docker + BS_EGRESS_GATE_URL via _exploit_one env_extras (dynamic seam, NOT static .mcp.json). ALIGNED (no change): evidence-mcp put_artifact(...raw_bytes_b64,oracle_method)/append_audit_entry match Step 7; run_poc_template(finding_id,scope_token,egress_allowlist,template_id,steps,...) params match spec call. TASKS: T1 migration 13_phase2_exploit_failed_status.sql (3 ADD VALUE IF NOT EXISTS) + test_schema_vs_spec_contract enum assert; T2 exploit-agent.md reconcile (Firecracker→docker, result.verdict, +6 mcp grants, Step8 microVM→docker) + orchestrator _exploit_one env_extras (SANDBOX_DRIVER=docker, BS_EGRESS_GATE_URL default http://localhost:9090) + validator-agent.md assert-clean; T3 hermetic tests/test_exploit_one_wiring.py (mock _run_agent, assert env + spec-consistency static checks); T4 human-verify FULL live dry-run (seed hypothesis + scope JWT + approve.py T2 token; success→exploit_pending_validation+evidence, denied→exploit_failed_oos, no orphans). files_modified: infra/sql/13_*.sql(new), .claude/agents/exploit-agent.md, scripts/orchestrator.py, tests/test_exploit_one_wiring.py(new), tests/integration/test_schema_vs_spec_contract.py. BOUNDARIES: sandbox-mcp/infra-sandbox/evidence-mcp/dedup-mcp/01_schema+migrations00-12/validation-domain UNTOUCHED; docker substrate ONLY (no Firecracker beyond deferral note); no new deps; no static .mcp.json SANDBOX_DRIVER.

02-02 SHAPE: STANDARD (complex→bundled runner+agent SPLIT to keep ≤3 tasks), 3 tasks + 1 checkpoint:human-verify, autonomous=FALSE, AC-1..AC-6, depends_on=[]. SCOPE = community sandbox docker-driver ONLY (Firecracker→Phase 3 task 214). DECISIONS (AskUserQuestion 2026-05-31): SPLIT runner(02-02)/agent(02-03); egress enforcement = iptables egress-gate sidecar (NET_ADMIN, deny-by-default, per-container, keyed by container_id) NOT HTTP-proxy; exploit_failed_* → ENUM migration (deferred to 02-03, NOT this plan). RECON GROUND TRUTH (verified 2026-05-31, 3 tools agree): DockerDriver.run() is a SCAFFOLD returning Verdict.ERROR — needs real impl. RunResult ALREADY has Verdict.EGRESS_BLOCKED + blocked_egress + egress_attempts → NO types.py schema change. local.py = reference for bounded-streaming/timeout/env-strip (REUSE pattern, DON'T edit). server._select_driver ALREADY routes SANDBOX_DRIVER=docker (+tested) → driver-selection DONE. sandbox-mcp has NO docker SDK → drive `docker` CLI via asyncio subprocess (add httpx for gate calls only). finding_status ENUM ALREADY has exploit_attempt/exploit_candidate/approval_pending_t2 + 06 added exploit_pending_validation; ONLY exploit_failed_* missing (→02-03). infra/sandbox/ does NOT exist yet; compose=infra/docker/docker-compose.yml (no sandbox/egress svc yet). TASKS: T1 DockerDriver.run/snapshot (container exec, fail-closed-on-no-gate, --cap-drop=ALL, bounded output, verdict map incl egress_blocked, cleanup try/finally); T2 bs5-egress-gate sidecar + bs5/sandbox-runtime pinned image + infra/sandbox/ + compose wiring; T3 hermetic tests (mock docker subprocess + fake gate); T4 human-verify live egress-block proof. Apply MUST re-verify contracts vs disk FIRST (planning-corruption seen in 02-01/01-02 sessions).
APPLY 02-01 (2026-05-31): both tasks qualified PASS, all AC-1..AC-7 met. dedup-mcp 61 passed/1 skipped; control-plane test_verify_finding 33 + test_dedup_gate green (41 combined); ruff clean on touched. SHIPPED: T1 dedup gate raised to recall≥0.95 ∧ precision≥0.90 (network-free) — root cause of bad precision was singleton text collisions (measured 0.002), fixed by per-finding lexical NONCE in the FIXTURE (K=20, T2 tier → 1.0/1.0, fp=0); production server.py thresholds untouched; non-degenerate guard added; test_recall.py left green. T2 dedup_gate.py (DedupStore on SHARED pool, no 2nd pool) + verify_service two hooks: pre-oracle exact-dup park ('duplicate', oracle never awaited, no evidence, transient-release+reraise) + post-validate register (fpr FIRST, embedding SECOND swallow-on-fail, no revert); 01-06 ordering untouched; validator.md aligned (order + param-in-identity + register_embedding + L2→02-04/L3→02-03 notes). DRIFT CAUGHT (planning-corruption guard): plan+audit-A sketched parameter fold as `path?param` but fingerprint._normalize_path STRIPS `?...` → would collapse distinct-param bugs (AC-6 violation); fixed by folding param as normalization-surviving path SEGMENT `::param::<v>`; check+register share one _fp; pinned by test. DEVIATIONS (justified, no scope change): (1) workspace source added to ROOT [tool.uv.sources] not control-plane (matches oracle/politeness pattern); (2) check_exact_duplicate returns (canonical,fp) tuple not str|None (AC-7 log needs fp); (3) embed text = fixed f"{cwe}\n{url}\n{parameter}" not raw_finding (05 col defaults '{}', JSONB order non-deterministic); (4) added test_dedup_gate.py (AC-6 home). DEFERRED per boundaries: L2 auto-park→02-04, L3 pre-submission→02-03, live worker verify+dedup smoke→deploy gate (01-06/01-09 posture). PRE-EXISTING/UNRELATED: respx dev-extra un-synced → test_h1_client.py/test_scope_ingest.py collection errors (files untouched by 02-01).

PHASE 2 RE-SLICED 4→5 plans (2026-05-31, AskUserQuestion-confirmed): 02-01 validator+dedup tail (THIS); 02-02 exploit+sandbox (docker-driver, Firecracker→P3 task 214, research-gated); 02-03 ai-vuln-hunter+cloud-recon+reporter (incl L3 pre-submission cross-check); 02-04 anti-slop SLOs+killswitch+AnyPoC guards (incl semantic L2 auto-triage); 02-05 XBOW benchmark+publish. DECISIONS: keep repo layout (control-plane/domains + mcp/, NOT packages/community/); refusal open-weight fallback STAYS ARCHIVED (A4 verdict) = in-process retry-once.

02-01 SHAPE: STANDARD, 2 tasks, autonomous=true, AC-1..AC-5. depends_on=[]. ⚠ TOOL-OUTPUT CORRUPTION during planning: two intermediate "Reads" (21_findings_status_check.sql / 03_dedup_diskann.sql / 20_dedup_semantic.sql + a ck_findings_status CHECK constraint) were FABRICATED — those files DO NOT EXIST. Ground truth re-confirmed across 3 independent tools (Bash cat + ctx_execute + ctx_execute_file, all agree). RECON GROUND TRUTH (verified 2026-05-31): validator ~80% built (01-06: control-plane/domains/validation/{dispatch.py 98, services/verify_service.py 148}, 5 oracles, 29 tests; verify FSM claim→oracle→evidence→validated, transient-release retry; ZERO dedup calls = the seam; parks = RETURN-label only, leave status validation_pending, only CLAIM/VALIDATED/REJECTED/RELEASE write status). dedup BOTH layers built (mcp/dedup-mcp): L1 exact=DedupStore.lookup←check_duplicate; L2 semantic=DedupStore.semantic_search over findings.embedding, ALREADY program-scoped, excludes archived/rejected/duplicate ←check_semantic_duplicate; register_finding (exact ON CONFLICT DO NOTHING) + register_embedding exist; NO find_similar. SCHEMA (infra/sql/NN_name.sql, 13 files 00–12): status is ENUM finding_status (01_schema:16) which ALREADY CONTAINS 'duplicate' (line 30) + 'dedup_check' → NO status migration. findings has NO title/description col (cols: program_handle,platform,cwe,url,parameter,status,evidence_hash,oracle_method,validator_model,deduplication_key,embedding) → embedding text source = raw_finding (05) or compose cwe+url+parameter. findings.embedding indexes EXIST: HNSW (01_schema:184) + DiskANN (10) → NO index work. recall gate exists (test_recall.py ≥0.90, BagOfWordsEmbedder, no net) but NO precision gate. TASKS: T1 raise gate→recall≥0.95+precision≥0.90 (add compute_precision to recall_fixture + new test_dedup_layers.py; tune FIXTURE not thresholds); T2 wire L1 exact into verify_service (new dedup_gate.py adapter on existing dedup impls; pre-oracle check_exact_duplicate→UPDATE status='duplicate' no-exec/no-evidence, transient→_RELEASE+reraise; post-validate register fpr+embedding WARNING-on-fail no-rollback; resolve embedding text source; control-plane dep dedup-mcp + uv sync --all-packages; tests; validator.md align). DEFERRED: L2 semantic auto-park→02-04; L3 pre-submission cross-check→02-03. Apply MUST re-verify contracts vs disk FIRST (corruption seen). Audit gate ENABLED → recommend /paul:audit before /paul:apply.
01-10 SHAPE: STANDARD, 3 tasks, autonomous=FALSE (T3 = checkpoint:human-action — public push + GitHub release needs operator GitHub auth + target-repo decision). depends_on=[01-08,01-09]. files_modified= .paul/paul.json, .paul/PROJECT.md, CHANGELOG.md(new), README.md (metadata+docs ONLY — no code/schema/dep/license-text). T1 reconcile version 0.0.0→0.1.0 in paul.json+PROJECT.md (pyproject ×2 ALREADY 0.1.0) + CHANGELOG.md [0.1.0] (SUMMARY-backed, no invented features) + README release section. T2 SECRET-SAFETY GATE (M1, BLOCKING): scan tree AND git history for .env/keys/*.pem/*.jwt/RS256-priv/HATCHET_CLIENT_TOKEN/SCOPE_WEBHOOK_URL/discord webhook/sk-ant-/AKIA — LEAK→ABORT (no tag/push), redact value, surface to operator; history-rewrite is operator-approved follow-up NEVER auto. Confirm LICENSE+LICENSE-AGPL at root. T3 human-action: operator confirms PUBLIC repo slug (origin=itsallinthethrees/ai5 NOT writable/NOT target; fork=nilhemdot/ai7 personal) → tag v0.1.0 annotated + push + gh release create. RELEASE FACTS: LICENSE/AGPL/APACHE/LICENSES.md present; no CHANGELOG yet; no tracked .env/.pem/.jwt (the *token* hits = hook scripts + test fixture, not secrets).
Status: 01-09 LOOP CLOSED. All 7 ACs PASS LIVE on local Docker. SHIPPED: Dockerfile.control-plane (bbscope v2 pinned-commit @2d3bae66 + Go-checksum-DB integrity gate; chromium/Playwright 1.58.0 baked, pinned by uv.lock) + Dockerfile.recon (COPY mcp/ fix for workspace members; subfinder/httpx/katana confirmed) + .gitattributes (new, `* text=auto eol=lf` + LF first-party + -text binaries/keys) + scripts/live_smoke.sh (new, scope-JWT-gated smoke runner, M1/M3 enforced). LIVE smoke results: AC-1 bbscope invocable + chromium 145.0.7632.6 on RUNNING worker; AC-2 chromium executed injected script in-container (XSS oracle live); AC-3 worker registered 0 errors; AC-4 webhook POST→HTTP 204 (redacted); AC-5 passive subfinder→50 subdomains of *.spotify.com (rps=2, JWT-derived); AC-6 working-tree CRLF=0; AC-7 target derived from scope JWT ONLY, abort-on-invalid enforced. Security invariants held: M1 (recon target from JWT only), M2 (bbscope pinned+checksum-verified, chromium pinned by lockfile), M3 (token/webhook never echoed/committed, webhook redacted scheme+host, seeded recon_assets rows dropped on exit). Human-action checkpoint resolved: operator regen'd HATCHET_CLIENT_TOKEN, set Discord canary SCOPE_WEBHOOK_URL, named authorized program (pd-spotify *.spotify.com, JWT keys/scope_smoke.jwt). DEVIATIONS: recon image 1st build failed (uv sync exit 2, same workspace-member miss as control-plane) → COPY mcp/ fix; one transient proxy.golang.org TLS timeout on a recon rebuild (network, not code) → retry succeeded. SKILL AUDIT GAP: /e2e (SPECIAL-FLOWS required for Task 3) not formally loaded as a skill this session — live_smoke.sh IS the recon→scan→validate→report pipeline smoke (functionally satisfied), but the skill command was not invoked. Warn-only, non-blocking. 6 pre-existing integration failures (SubagentSimulator model_id drift, orchestrator.py:352) remain OUT of plan scope.
Last activity: 2026-05-31 — CREATED 02-03 PLAN (exploit-agent wiring onto 02-02 docker substrate). Loop: PLAN✓→APPLY○→UNIFY○. STANDARD, autonomous=false, 3 auto tasks + 1 human-verify (full live exploit dry-run), AC-1..AC-6. Recon caught: validator-agent.md already Firecracker-clean (the microVM line is inside exploit-agent.md Step 8). Next: /paul:audit 02-03 (audit gate ENABLED) → /paul:apply. PRIOR: UNIFIED 02-02 (sandbox docker-driver substrate). Loop CLOSED (PLAN✓→APPLY✓→UNIFY✓). 3 auto tasks qualified PASS + human-verify checkpoint approved (live egress proof). AC-1..AC-6 met; 02-02-SUMMARY.md written (AC-results table added at unify); re-verified vs disk at close: 40 tests pass, ruff clean. NOT phase-complete (Phase 2 = 2/6) → no transition; next /paul:plan 02-03. DockerDriver.run real impl (create→gate-register→start-a→report→deregister→rm; fail-closed RuntimeError on unset/unreachable gate before any networked container; --cap-drop=ALL; egress_blocked verdict map); bs5-egress-gate sidecar (deny-by-default iptables in DOCKER-USER, NET_ADMIN+docker.sock, host-net); bs5/sandbox-runtime:0.1.0 image built (alpine3.20, non-root); compose wired (gate svc + sandbox net + mcp-sandbox env/sock/depends). 40 tests pass, ruff clean. DEVIATIONS (no scope change): base images minor-tag not @sha256 (digest-pin=hardening follow-up, README-noted); mcp-sandbox image lacks docker CLI for in-container DOOD (follow-up, driver proven host-run); env read live in run() not module const (test-friendly), EGRESS_GATE_ENV const added, EGRESS_GATE_URL vestigial; +httpx dep; gate /report blocked-name best-effort dmesg w/ DROP-counter fallback. Boundaries held: types/local/protocol/_select_driver/validation/sql untouched; httpx only no docker SDK. Next: /paul:unify 02-02 → 02-03 (exploit-agent + exploit_failed_* enum). PRIOR: APPLIED 02-01 (dedup gate raise + verify_service exact-dedup); 02-01-SUMMARY.md.

Progress:
- Milestone v0.1 Community Edition MVP: [██████████] 100% COMPLETE (Phase 0 + Phase 1; v0.1.0 released 2026-05-31)
- Phase 0: [██████████] 100% (4/4 plans complete)
- Phase 1: [██████████] 100% (10/10 done — 01-01..01-10; v0.1.0 PUBLIC release shipped)
- Phase 2: [███░░░░░░░] 33% (2/6 plans complete — 02-01 validator+dedup tail, 02-02 sandbox docker-driver substrate)

## Loop Position

Phase 2 loop (02-03) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — 02-03-SUMMARY.md written. exploit-agent wired onto 02-02 docker substrate; AC-1..AC-6 PASS; 44 tests; live dry-run GREEN (ALLOWED→exploit_pending_validation, DENIED→exploit_failed_oos). Phase 2 = 3/6 (02-04/05/06 unplanned) → NOT phase-complete, no transition. Next /paul:plan 02-04. ORIG-APPLY: T1-T3 PASS (migration 13 live+idempotent+enum-contract; exploit-agent.md→docker substrate +6 mcp grants +result.verdict; orchestrator SANDBOX_DRIVER=docker handoff; 44 hermetic tests pass; ruff clean). T4 human-verify GREEN via live one-shot dry-run (scripts/exploit_dryrun.py + exploit_dryrun_netns.sh): real DockerDriver+gate, ALLOWED→success→exploit_pending_validation, DENIED→egress_blocked→exploit_failed_oos, rows cleaned, no orphans. UNPLANNED DEFECT FIX (caught by the live runner, beyond original files_modified): register-before-start 409 — bridge IPs bind at `docker start`, so the gate inspect saw no IP at /register. Fixed: DockerDriver pins static --ip at create (DockerDriver._reserve_ip, deterministic in-subnet hash + attempt-retry) + gate _container_ip reads IPAMConfig.IPv4Address fallback (readable pre-start). Preserves fail-closed (iptables before payload runs). Gate image rebuilt. Next: /paul:unify 02-03.]
```
Phase 2 loop (02-02) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — sandbox docker-driver substrate; AC-1..AC-6 PASS; 40 tests; egress-gate live proof approved. NOT phase-complete (2/6) — next 02-03]
```
Phase 2 loop (02-01) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — validator + 3-layer dedup tail; dedup gate recall≥0.95∧precision≥0.90; exact-dup wired into verify_service]
```

Phase 1 loop (01-10) — COMPLETE + PHASE TRANSITION:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — Phase 1→2 transition executed; v0.1 milestone complete]
```

Phase 0 loop (00-04) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop complete — Phase 0 transition executed]
```

Phase 1 loop (01-01) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — DB foundation shipped + verified]
```
Phase 1 loop (01-02) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — Hatchet v1 runtime shipped + verified]
```
Phase 1 loop (01-03) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — evidence write path shipped + verified live]
```
Phase 1 loop (01-04) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — federated scope ingestion shipped + verified LIVE (798/4104/4104)]
```
Phase 1 loop (01-05) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — scope-diff notification delivery shipped + verified]
```
Phase 1 loop (01-06) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — deterministic verifier wired + tested (29/29)]
```
Phase 1 loop (01-07) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — recon politeness token-bucket shipped + tested (49 green)]
```
Phase 1 loop (01-08) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — one-line installer shipped + verified (10/10 ACs, harness 26/26, e2e_verify rc=0)]
```
Phase 1 loop (01-09) — COMPLETE:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop closed — live deploy-gates closed on local Docker; 7/7 ACs PASS; bbscope+chromium baked, CRLF swept, scope-JWT-gated smokes green]
```
Phase 1 loop (01-10) — PLAN created:
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○     [Plan created — public AGPLv3 v0.1.0 release; standard, autonomous=false (T3 human-action: public push + GitHub release); 3 tasks, 3 ACs; secret-scan gate T2 BLOCKING. UNIFY triggers Phase 1→2.]
```
Note: 01-09 line SPLIT (2026-05-31) — 01-09 = live gates (local Docker smokes) + image bake + CRLF sweep; 01-10 (NEW) = public AGPLv3 release (tag+README+GitHub). Downstream +1 → 10 plans. 01-10 UNIFY (not 01-09) triggers Phase 1→2.
Note: 01-08 line SPLIT (2026-05-31) — original 01-08 "installer + public release" → 01-08 = autonomous one-line installer (install.sh + unattended secret-gen + offline test + Coolify recipe doc); 01-09 = live deploy-gate closure (bbscope+chromium image bake + parked worker/recon→scan/webhook smokes) + public AGPLv3 release (tag + GitHub release, human-action checkpoints). Downstream +1 → 9 plans (01-01..01-09). 01-09 UNIFY (not 01-08) now triggers Phase 1→2.
Note: 01-02 line SPLIT — 01-02 = Hatchet v1 only; 01-03 = evidence store (R2). Downstream +1 (was 7).
Note: 01-04 line SPLIT (2026-05-29) — old single 01-04 (ingestion + RS256 + notifications) → 01-04 = ingestion completion (bbscope+projectdiscovery+scheduled poll+reconcile); 01-05 = scope-diff notification delivery (NEW). Downstream +1 → 8 plans (01-01..01-08). RS256 JWT already built (v5 carryover) → reconcile-only inside 01-04, not a standalone plan.

## Accumulated Context

### Decisions

| Decision | Phase | Impact |
| 2026-05-31: Enterprise audit performed on 01-10-PLAN.md. Verdict: conditionally acceptable → enterprise-ready after fixes. Applied 4 must-have (AC-2/Task2 MACHINE secret gate exits non-zero on leak — T3 mechanically blocked; AC-4/Task3 tag-collision preflight `git tag -l`+`git ls-remote`, NEVER `-f`/`--force` a published tag; partial-push recovery — `gh release` failure retries release-only, records half-state; Task3 step1b operator reviews `git log --oneline`+manifest count+gate exit before approve) + 4 strongly-recommended (resolve real license filenames via `git ls-files|grep -i licen`, don't assume LICENSE-AGPL + assert LICENSES.md; scan .env.example for real-secret needles; prefer gitleaks else record "grep-needle fallback"; AC-4 SUMMARY records release commit SHA+tag SHA+slug+scan method+approval timestamp). Deferred 3: GPG-signed tags (D1), CHANGELOG compare-URL backlinks (D2 — need slug), machine-readable SBOM (D3 — Phase-2 supply-chain). AUDIT.md written. | Phase 1 | 01-10 release plan strengthened to enterprise standard: no silent half-publish, no force-overwrite of a published tag, machine-enforced secret gate, audit-defensible release evidence. Loop still at PLAN (audit is a PLAN sub-step). |
|----------|-------|--------|
| AGPLv3 Community + proprietary Enterprise + Apache 2.0 shared primitives | Init | Governs all licensing and repo layout |
| Deterministic verifier is the moat — no submission without evidence artifact | Init | Shapes Phase 1 scope and oracle TPR=1.0/FPR=0.0 requirement |
| SOC 2 clock must start at Phase 3 launch (Week 19), not enterprise launch | Init | Hard constraint on Phase 3 exit criteria |
| Position as AEV inside Gartner CTEM, not "bug bounty tool" | Init | Shapes Phase 4 marketing, pricing, and competitive framing |
| 2026-05-27: Enterprise audit on 00-01. Applied 3 must-have + 2 strongly-recommended. Verdict: conditionally acceptable → ready | Phase 0 | Plan now enforces full doc coverage, inline provenance, cost-overrun stop |
| 2026-05-27: 00-01 edit scope = v5 plan + research/ + support docs (NOT v6 docs, which hold the corrections as source-of-truth). v5 build plan edited in place despite being superseded, for fact-check consistency | Phase 0 | Stale claims corrected wherever they appear as live assertions; v6 docs left as correction record |
| 2026-05-27: Git committer identity set repo-local (3x3by3@gmail.com / nilhem) to enable baseline commit | Phase 0 | One-time; no global config touched |
| 2026-05-27: Monorepo approach = HYBRID (keep uv workspace, layer pnpm/Turborepo on top; no physical code move). Repo was already a uv workspace, not greenfield | Phase 0 | 00-02 scaffolds license roots + task layer; physical reorg deferred |
| 2026-05-27: Phase 0 split 2→3 plans (00-03 = OpenFeature + custom Unleash provider, separated due to no official Python provider per brief) | Phase 0 | Feature flags own plan |
| 2026-05-27: Audit on 00-02 added license-text integrity (verbatim canonical), LICENSES.md code↔license map, AGPL §13 network-use clause | Phase 0 | License posture audit-defensible before code ships |
| 2026-05-27: Audit on 00-03 — tier-enterprise flag is a config/rollout gate, NOT an authz boundary. Entitlement enforced server-side vs signed plan. Provider: TLS + secrets token + fail-closed on malformed response; context untrusted | Phase 0 | Prevents flag-as-authz privilege-escalation; sets the doctrine for all later tier gating |
| 2026-05-29: Audit on 00-04 — 5 must-have + 6 strongly-rec applied; 4 deferred. Verdict: conditionally acceptable. Caught 2 release-blocking license defects (foreign/.venv AGPL-stamping; AGPL-vs-Apache root collision) + unsound import-ban canary test. Doctrine: SPDX sweep uses six-root ALLOW-LIST (not git-walk+deny), shebang/PEP-263-cookie ordering, byte-equality idempotency proof, canary inside contract source_modules w/ trap-cleanup | Phase 0 | License sweep audit-defensible; import-ban enforcement sound. Residual risk: APPLY must encode allow-list, not regress to git-walk |
| 2026-05-29: Phase 1 split — 01-01 line (5 subsystems) split into 01-01 DB foundation (PG17 custom image pgvector+vectorscale+pg_search) + 01-02 Hatchet v1 + evidence store; downstream plans renumbered +1 (now 6 plans). Matches PAUL 2-3 task sizing | Phase 1 | Single-concern plans; DB foundation isolated from workflow/evidence wiring |
| 2026-05-29: Enterprise audit on 01-01 (DB foundation). Applied 4 must-have + 4 strongly-rec; deferred 3. Verdict: conditionally acceptable (was not-acceptable as written). Caught 2 release-blocking SQL defects — BM25 migration indexed non-existent findings.title/description cols, and used the DEPRECATED `paradedb.create_bm25()` API (current = `CREATE INDEX … USING bm25 … WITH (key_field='id')`, verified Context7). Also: zero version pins despite "reproducible" claim (pinned pgvector 0.8.0 / vectorscale 0.9.0 / pg_search v0.23.x + fixed base tag); Alpine/musl build unverified (added glibc bookworm fallback, AC-1 = empirical gate); incomplete cargo-pgrx invocation (match version + `pgrx init`); CONCURRENTLY in initpath → plain CREATE INDEX (INVALID-index masking); added AC-7 pins, AC-8 idempotency-on-re-apply, AC-3 functional .so smoke. Doctrine: migrations must target verified-existing columns; pg_search uses CREATE INDEX USING bm25 (not legacy CALL); pin all extensions+base; first-boot init = no CONCURRENTLY; verify extensions functionally not by catalog row | Phase 1 | DB foundation audit-defensible + reproducible; plan can now satisfy its own ACs at apply time |
| 2026-05-29: APPLY 01-02 (Hatchet v1). hatchet-lite v0.86.18 + dedicated hatchet-postgres in compose; all 8 ACs verified LIVE (engine healthy, worker registered bs-heartbeat, triggered run returned result/SUCCEEDED). 7 deviations: (1) topology = hatchet-lite Postgres-only (plan said engine+RabbitMQ) — lite uses PG as both store+queue, AC-8 isolation kept, 1 fewer container; (2) replaced a PRE-EXISTING broken `hatchet` service (engine pointed at app DB, no broker, + YAML bug: networks nested under healthcheck); (3) gRPC 7070→7077 (lite default); (4) `uv sync --all-packages` REQUIRED — plain root sync exits 0 but skips workspace-member deps; (5) sdk pin 1.33.6 (PyPI latest; trap #2 floor >=1.33.5; no `__version__` attr — use importlib.metadata); (6) client token session-ephemeral via `hatchet-admin token create --config /config` (path is /config not /hatchet/config) for seeded Default tenant 707d0855-…, NOT committed; (7) healthcheck `/api/ready`. HARNESS NOTE: severe tool-output corruption this session (fabricated/merged results + parallel-cancel cascades); first APPLY pass fabricated success w/ nothing on disk — caught, redone strict-sequential, every claim re-verified against disk/sandbox. | Phase 1 | Durable-execution backbone live. Doctrine: hatchet-lite for solo self-host (PG-only, isolated DB); workspace deps need `uv sync --all-packages`; self-host worker needs HATCHET_CLIENT_TOKEN + TLS_STRATEGY=none; under harness corruption switch to strict-sequential + re-verify on disk. Open: in-container worker token wiring (later plan) |
| 2026-05-29: Enterprise audit on 01-03 (evidence store / R2). Applied 3 must-have + 3 strongly-rec; deferred 2. Verdict: conditionally acceptable (was not-acceptable). Caught release-blockers: (M1) genesis prev_hash claimed 32 zero bytes but migration 03 + HashChainService use b"" 0-byte — plan would fail own chain-verify; (M2) moto cannot reproduce R2 CRC-header rejection so moto round-trip ≠ proof of trap-#5 fix — AC now asserts Config values by introspection; (M3) idempotent re-record skipped dup artifact row but would still append a 2nd audit entry → crashed-task replay could pad/fork the tamper-evident chain (audit append now only on NEW insert). Strongly-rec: scope_token_jti required non-empty (provenance), blob-PUT-before-commit + orphan-blob-acceptable semantics, raw_bytes size cap (Hatchet gRPC ~4 MiB). Deferred: oracle_verdict gating (recording attempts = legit audit trail), aioboto3 pooling (Phase 2). Doctrine: hash-chain genesis = b""; moto proves byte-fidelity not R2-checksum-compat (assert config + deployment smoke); idempotency must cover the audit append not just the row. | Phase 1 | Evidence write path audit-defensible; chain replay-safe |
| 2026-05-29: APPLY/UNIFY 01-03 (evidence store / R2). All 6 ACs verified LIVE (4/4 tests: 2 unit + 2 integration vs bs-postgres; worker registered record-evidence; trigger SUCCEEDED w/ full payload; blob on disk; chain canonical-verifies, genesis=b""). 4 deviations: (1) ON CONFLICT(content_hash) IMPOSSIBLE — no UNIQUE constraint + schema locked → per-finding pg_advisory_xact_lock SELECT-guard keyed (finding_id,content_hash), audit-append only on NEW insert; (2) pyproject.toml edited (NOT in files_modified) — added moto[server]>=5.0 dev dep + registered `integration` marker in control-plane [tool.pytest.ini_options]; (3) moto mock_aws unreliable w/ aiobotocore → ThreadedMotoServer (real HTTP localhost); (4) live trigger ran EVIDENCE_BACKEND=local (no R2 creds; R2 S3 path covered by moto unit test). Doctrine: idempotency w/o UNIQUE = advisory-lock SELECT-guard; service self-registers jsonb codec on acquired conn (HashChainService passes dict→jsonb, no codec existed anywhere); aiobotocore needs ThreadedMotoServer not mock_aws; PostToolUse ruff-autofix STRIPS unused imports between edits — add import only once its usage edit lands. | Phase 1 | Evidence write path live + audit-defensible. Open: evidence-mcp↔control-plane divergence deferred; in-container worker token wiring; pre-existing ruff debt |
| 2026-05-29: PLAN 01-04 + split. Scope recon found scope_management ~70% pre-built (v5 carryover, untouched by Phase 1): arkadiyt+hackerone clients, jwt_issuer RS256 issuer/validator, 575-line ingest_service w/ detect_scope_changes, normalize ×5 platforms, scope_events diff classes, scope-mcp TS validation, 3 test files. Genuine gaps: (1) bbscope v2 integration MISSING, (2) projectdiscovery integration MISSING, (3) notification DELIVERY missing (events persisted, no channel), (4) no Hatchet scheduled poll task, (5) reconcile vs 01-01 custom-PG schema (scope code predates Phase 1 DB rebuild). 5 concerns → COMPLEX → SPLIT: 01-04 = ingestion completion (bbscope+projectdiscovery+scope_poll task+reconcile, 3 tasks); 01-05 = notification delivery. RS256 already shipped → reconcile-only, not rebuilt. | Phase 1 | Single-concern plans; ingestion isolated from notification delivery. Traps in play: #3 bbscope poll/db, #8 projectdiscovery dist/data.json `programs` key, #2 Hatchet v1 cron, #15 RS256 verify, #10 Bugcrowd cookie |
| 2026-05-29: APPLY/UNIFY 01-04 (federated scope ingestion). All AC-1..AC-5 verified (13/13 scope tests; ruff clean ×6 files). LIVE: worker registered scope-poll; trigger returned {programs:798,scopes:4104,events:4104} into bs-postgres; re-trigger events=0 (idempotent). 5 deviations: (1) generated keys/ RSA-4096 keypair (gitignored PEMs absent → test_scope_jwt) — env reconcile, NOT files_modified; (2) DEFAULT_DATABASE_URL (bountystrike:bountystrike) ≠ live bs-postgres (user `bs`/pw `verify_local_pw_01_01`, 01-01 throwaway) — live trigger needs explicit DATABASE_URL; flag for installer 01-08; (3) live trigger ran sources=["projectdiscovery"] only (bbscope binary not installed→01-08; arkadiyt skipped for determinism; both covered by unit tests); (4) scope_poll per-source commit (reuses committing ingest_*), not single atomic txn — documented; (5) _ingest_federation_program gained `source` param (in-scope helper extension). Doctrine: bbscope v2 = `poll`+`db -p X -o json` (+`--oos`), NOT v1 `h1 -t`; pd- handle namespacing prevents programs-PK collision; bare-target sources reuse per-platform normalizers via per-platform key-shape map; ruff-autofix strips unused imports between edits (land usage with/before import). RS256 confirmed in jwt_issuer (trap #15). | Phase 1 | Scope data foundation complete (3 federated sources + scheduled poll). Open: bbscope/arkadiyt live paths unverified (binary→01-08); DEFAULT_DATABASE_URL creds mismatch; notification delivery → 01-05 |
| 2026-05-29: Enterprise audit on 01-05 (scope-diff notification delivery). Applied 5 must-have + 4 strongly-rec; deferred 3. Verdict: conditionally acceptable (was not-acceptable). Caught 2 release-blockers: (M1) migration must BACKFILL existing rows notified_at=now() — else first run retro-floods the webhook with ~4104 stale 01-04 events (column added NULL on all history); (M2) single limit=200 POST breaks Discord content 2000-char cap AND dribbles backlog 200/6h-poll (~5d to drain fresh DB) → BATCH_SIZE=25 + length-capped summary + drain loop. Also: (M3) SCOPE_WEBHOOK_URL is secret (Slack/Discord embed token) — never log/return/raise full URL, redact to scheme+host; (M4) false "exactly-once" claim → at-least-once (POST-then-commit redelivery window documented); (M5) silent-failure → WARNING w/ status+pending count, return {notified,pending}. Strongly-rec: FOR UPDATE SKIP LOCKED (cron+manual overlap), retry 429/5xx not just transport, call-site fail-open wrap, http(s) scheme validation. Deferred: per-program routing/email (scoped out), metrics→Langfuse (Phase 2/3), HMAC payload sig (Phase 4). Doctrine: feature-add migrations on a populated table must backfill the new delivery-marker as already-done (go-forward-only); outbound webhook URLs are credentials; alerting controls must fail loud + observable; bound payloads to the strictest provider cap (Discord 2000) + drain in-run not across polls. | Phase 1 | Notification delivery audit-defensible; will not retro-flood / leak / silently fail on first deploy |
| 2026-05-29: APPLY 01-05 (scope-diff notification delivery). All ACs verified; 3/3 tasks qualified. 5 deviations: (1) AC-6 backfill verified LIVE vs bs-postgres (4104 rows backfilled, re-apply UPDATE 0, NULL=0) — migration .sql is PG-specific so unit test asserts backfill SEMANTICS portably on SQLite, not the .sql itself; (2) tasks.py gained `import structlog` + module logger (plan snippet said `log.warning` but tasks.py had no logger) — additive, append-only, in-boundary; (3) scope_poll ingest-accumulation loop narrowed from `for key in totals` to explicit ("programs","scopes","events") tuple so the new notified/pending keys stay delivery-authoritative (benign); (4) deliver_pending calls session.rollback() before counting pending on failure — needed to release FOR UPDATE SKIP LOCKED row locks held in the same txn (impl detail beyond plan text); (5) live worker-trigger of scope_poll NOT run — module import of tasks.py needs HATCHET_CLIENT_TOKEN (pre-existing client.py gate, same as 01-04); verified via py_compile + AST wiring-check + 9 unit tests instead. Live end-to-end webhook POST deferred (no token regen + no real webhook endpoint this session). Doctrine: notification service must be dialect-portable (with_for_update(skip_locked=True) no-ops on SQLite, ORM update().where(id.in_()), Python datetime.now(UTC) not SQL now()); FOR UPDATE locks require rollback-before-recount on the fail path; secret webhook URLs redact to scheme+host in every log line. | Phase 1 | Scope-change signal now leaves the DB. Open: live webhook POST + worker-trigger unverified (token+endpoint → 01-08 installer / a live smoke); duplicate-on-crash redelivery window accepted (at-least-once, documented) |
| 2026-05-31: APPLY 01-06 (deterministic verifier wiring). All 3 tasks qualified PASS; 29/29 unit tests; ruff clean on all touched files. SHIPPED: control-plane new `validation` domain — dispatch.py (resolve_oracle/normalise_cwe, frozen OracleSpec, OPEN_REDIRECT_CANARY const; CWE-79/xss→oracle_xss, CWE-89/sqli→oracle_sqli, CWE-918/ssrf→oracle_ssrf, CWE-1336/"ssti"→oracle_ssti, CWE-601/open-redirect→oracle_open_redirect; CWE-94/CWE-74→None per audit S3) + services/verify_service.py VerifyFindingService.verify(finding_id): CLAIM(hypothesis→validation_pending, $1::uuid, RETURNING)→noop_unclaimed if 0 rows (AC-5); resolve→unsupported parks (AC-4); null url/param guard parks (AC-11/S1); oracle in try/except, transient error RELEASES claim to hypothesis + re-raise (AC-9/M1); non-validated→rejected NO evidence (AC-2 moat); validated→resolve jti from scan_jobs, missing→park blocked_missing_scope_jti NOT reject (AC-10/M2), + platform/program_handle guard (blocked_missing_metadata, M2-class extension); raw_bytes=result.model_dump_json().encode() (S2); record()→status='validated'+evidence_hash=content_hash_hex ONLY after record returns (moat ordering). tasks.py +VerifyFindingInput +@hatchet.task("verify-finding") (pool min1/max4 for shared-pool re-acquire); worker.py registers verify_finding; orchestrator.py +trigger_verify_finding +_cli_trigger_verify_finding + "trigger-verify-finding" elif. pyproject +oracle-mcp dep. test_verify_finding.py 29 tests (FakePool/FakeConn/FakeEvidence + fake oracle — no DB/net/docker). DEVIATIONS: (1) pool max_size=4 not plan's 2 (verify re-acquires per write + evidence service acquires own conn; headroom vs deadlock); (2) added blocked_missing_metadata branch (platform/program_handle non-empty, same record()-input-completeness class as M2 jti guard — prevents ValidationError crash-strand); (3) tests use FakeConn not sqlite (service is asyncpg-specific $1::uuid/now(); fakes test logic dialect-free, cleaner than sqlite which can't run asyncpg SQL); (4) verify_service does NOT hold a conn across the oracle call (re-acquires per write) — safer than nested-acquire + frees pool during slow oracle. DEFERRED at apply: AC-8 XSS field-validation harness + live worker-trigger (needs docker+chromium+HATCHET_CLIENT_TOKEN; oracles stateless/untouched by 01-01 → deploy gate for 01-08, same posture as 01-04/05). Doctrine: asyncpg autocommit makes single-statement CLAIM the optimistic lock (concurrent worker gets 0 rows); CLAIM-before-side-effect MUST release on transient failure (M1); pass str finding_id with $1::uuid cast (asyncpg won't coerce str→uuid). | Phase 1 | Verifier LIVE in code: hypothesis→validated+evidence | rejected | parked, moat enforced by ordering, retry-safe. Open: live worker trigger + field-validation = deploy gate (01-08) |
| 2026-05-31: Enterprise audit on 01-06 (deterministic verifier wiring). Applied 2 must-have + 3 strongly-rec; deferred 3. Verdict: conditionally acceptable (was not-acceptable as written). Verified ALL asserted contracts vs live source FIRST (held): oracle sigs (url,param[,target]); record() kwargs + {content_hash_hex} return; finding_status enum (hypothesis/validation_pending/validated/rejected present); scan_jobs.scope_jwt_jti; Verdict literal; findings cols. Caught 2 release-blockers: (M1) CLAIM sets validation_pending BEFORE oracle runs → a transient oracle raise (interactsh down / no chromium / target unreachable) propagates → Hatchet retry's CLAIM matches 0 rows → noop → finding STRANDED forever in validation_pending (silent failure). FIX: wrap oracle in try/except, reset status='hypothesis' (release claim) + warning, re-raise for clean retry; AC-9. (M2) scope_token_jti punted to apply w/ wrong default "reject if empty" — job_id nullable + jti nullable + record() requires non-empty (raises before PUT); rejecting a validated verdict over a PROVENANCE gap terminally buries a real bug + corrupts confirmed-rate. FIX deterministic: missing jti → park validation_pending (NOT rejected, NOT crash) + warning; AC-10. Strongly-rec: (S1) null url/parameter guard before oracle (required param:str, cols nullable) AC-11; (S2) result.model_dump_json().encode() not json.dumps(model_dump()) (raises on non-JSON evidence values → strand); (S3) CWE-94/CWE-74→SSTI wrongly rejects real injection bugs SSTI probe can't validate → resolve to None (validation_pending, preserved for Phase-2 RCE), narrowed SSTI map to CWE-1336+bare "ssti" token. Deferred: D1 terminal error state after N retries (Phase 2 obs), D2 verify-outcome metrics/Langfuse (Phase 2/3), D3 validation_pending sub-state enum (schema change, out of no-migration scope). Doctrine: 'validation_pending'='parked/claimed-cannot-complete-now, NOT failure'; 'hypothesis'='fresh OR error-released-for-retry'; 'rejected'=ONLY a real non-validated oracle verdict (never provenance gap / missing input / crash) — keeps FSM honest for post-incident reconstruction. CLAIM-before-side-effect must release claim on transient failure or it poisons retry. | Phase 1 | Verifier FSM honest + retry-safe + audit-defensible; no stranded findings, no real bug rejected on infra/provenance failure |
| 2026-05-29: APPLY/UNIFY 01-01. Custom bs-postgres:pg17 built + ALL ACs verified live (PG17.10, 3 extensions active, DiskANN+BM25 apply+idempotent). 3 deviations: (1) glibc postgres:17-bookworm base not Alpine — pgrx+ParadeDB are glibc-only (plan-permitted fallback); (2) added `-c shared_preload_libraries=pg_search` to compose command — pg_search BM25 AM requires preload at server start (Context7-verified), absent from Task 2 spec; (3) `ruff check .` not clean — 40 PRE-EXISTING errors all in non-01-01 .py (control-plane/mcp/scripts/tests); 01-01 touched zero .py so introduced none; AC-6 "ruff exits 0" unsatisfiable w/o violating no-domain boundary. pgrx auto-matched to 0.16.1 via cargo metadata. | Phase 1 | DB foundation shipped. Doctrine: pg_search MUST be in shared_preload_libraries; custom PG ext images use glibc base + cargo-pgrx version-match. Open: pre-existing ruff debt needs separate triage (out of 01-01 scope) |

| 2026-05-31: APPLY 01-07 (recon rate-limiting token bucket). All 3 tasks qualified PASS; 8 new politeness tests + 41 existing recon tests green; ruff clean on 4 files. SHIPPED: root pyproject [tool.uv.sources] += politeness-mcp={workspace=true}; control-plane deps += politeness-mcp (uv sync --all-packages → 195 pkgs). probers.py HttpxReflectionProber gains keyword-only limiter/rps_for_host (both default None → AC-6 byte-identical ungated path preserved); when gated: host=(urlparse(url).hostname or "_nohost").lower() (M2 hostname-only key), rps=min(rps_for_host(host),MAX_RPS_CEILING) (S1/AC-7 clamp), acquire-before/report-after, granted=False→log recon.politeness.probe_skipped + return True (M1 FAIL-OPEN — candidate kept, oracle sole gate), acquire/report wrapped try/except→warn+fall-through-to-ungated-GET (S2). Added module structlog logger to probers.py (none existed). __main__.py builds one TokenBucketLimiter(default_rps=float(scope_filter.default_rps)) per run + injects rps_for_host=scope_filter.rps_for_host (relaxed_hosts now LIVE) + log.info recon.politeness.enabled; ReconService order / tool_runner -rate-limit flags / Protocol untouched. test_recon_politeness.py: real limiter+ManualClock+fake_sleep (mirror test_bucket.py), monkeypatch httpx.AsyncClient.get, structlog.testing.capture_logs for warnings — AC-2 order, AC-3 override(20/5), AC-4 429 backoff, AC-5 fail-open(True+GET not called+warn), AC-7 clamp(50), AC-6 ×2 ungated. DEVIATIONS: (1) probers.py had NO logger → added structlog.get_logger (plan said "existing logger"; additive, import landed with use). __main__.py import structlog + TokenBucketLimiter WERE stripped by ruff-autofix (added before logger/limiter usage edits landed → unused at strip time) → NameError caught at QUALIFY, re-added once usage present; the exact ruff-strip-between-edits trap STATE warned of; (2) AC-5 test pre-creates bucket via direct limiter.acquire then forces tokens negative (mirrors test_bucket excessive-wait test) — prober's own acquire can't easily provoke >30s wait on a fresh bucket; (3) NO live worker/orchestrator trigger (recon launches as subprocess via _phase_recon, not a Hatchet task — by design, unlike 01-03/04/05/06). DEFERRED at apply (per plan boundaries): dnsx/naabu stages (Phase 2), binary-stage relaxed_hosts batching (can't throttle a binary's internal loop token-by-token), Redis multi-process backend, skip-rate metrics (Phase 2/3). Doctrine: politeness gate may DELAY/SKIP a probe, NEVER discard a candidate (fail-open keep+warn); per-host key=lowercased hostname only; rps always clamped to ceiling; limiter on the hot path must be fully fail-open (acquire+report wrapped). | Phase 1 | Recon's one unbounded in-process egress path (reflection prober) now per-host throttled w/ 429 backoff; rps_for_host activated; fail-open so rate-limiting never costs recall. Open: live recon→scan smoke = deploy gate (01-08) |
| 2026-05-31: Enterprise audit on 01-08 (one-line installer). Applied 4 must-have + 4 strongly-rec; deferred 4. Verdict: conditionally acceptable (was not-acceptable). Verified contracts vs live source FIRST: .gitignore has .env+keys/ but NOT .env.backup*; health_check.sh exits 0/1/2 under set -e; umask 022; compose NO-default vars = ONLY POSTGRES_PASSWORD+REDIS_PASSWORD (the other 4 secrets carry `:-` hardcoded defaults). Caught release-blockers: (M1) umask 022 → install.sh writes .env 0644 = world-readable API keys/DB pw/webhook tokens; seed_env must chmod 600 + no .env.backup*. (M2) HATCHET_POSTGRES_PASSWORD/HATCHET_COOKIE_SECRET/LANGFUSE_SECRET/LANGFUSE_SALT only `:-`-defaulted in compose → stack boots on SOURCE-VISIBLE secrets unless .env overrides; orig AC-5 (no-default parity) was trivially already-satisfied + missed this. seed_env now overrides ALL 4 + .env.example lists them + .gitignore += .env.backup* (init_wizard leak). (M3) health_check 0/1/2 under set -e → naive poll exits 0 on half-up stack; capture rc, treat 1/2=keep-waiting, timeout→non-zero naming svcs, never success banner while down; retries env-overridable. (M4) no openssl/bash preflight → partial .env with blank secrets; preflight before any write + post-seed `compose config -q` round-trip. Strongly-rec: (S1) gate docker auto-install behind explicit --install-docker (not bare --unattended) — curl|sh-as-root is the risk class this product hunts; (S2) `compose config -q` before up; (S3) README primary=git-clone form (curl|bash 404s pre-release→01-09); (S4) mixed-state .env AC. Deferred: Coolify live-validation (01-09), non-Ubuntu paths (Phase 3), secret rotation (init_wizard --update), compose-default removal (boundary-excluded, neutralized by always-override). Doctrine: installers that write secrets MUST chmod 600 + gitignore all backup variants + never rely on app-config baked-in default secrets (always override via .env) + fail LOUD on health-timeout (no exit-0 half-up) + preflight before partial writes + docker auto-install is opt-in not implied. | Phase 1 | Installer audit-defensible: no world-readable/predictable/leakable secrets, no silent half-up, no implicit curl|sh |
| 2026-05-31: Enterprise audit on 01-07 (recon rate-limiting token bucket). Applied 2 must-have + 2 strongly-rec; deferred 3. Verdict: conditionally acceptable (was not-acceptable). Caught release-blockers: (M1) AC-5 dropped xss-candidate on rate-limit exhaustion (probe→False = "unreflected" = discarded) → OUR throttle silently buries real bugs, same class as 01-06 M2. FIX fail-open: granted=False → probe returns True (KEEP candidate, oracle stays sole gate) + recon.politeness.probe_skipped WARNING. (M2) host=`hostname or url` fallback keys a fresh bucket per-URL → per-host throttle defeated when hostname None. FIX: `(urlparse(url).hostname or "_nohost").lower()`, never url; lowercase matches rps_for_host lowered lookup (one bucket per host). Strongly-rec: (S1) clamp rps=min(rps_for_host,MAX_RPS_CEILING) before acquire — untrusted relaxed_hosts JWT value must not crash recon (can't edit bucket.py); AC-7. (S2) wrap acquire/report fail-open — limiter bug must never crash recon pipeline. Deferred: skip-rate metrics (Phase 2/3), binary-stage bucketing (out of token-bucket-only scope), probe_skipped finding-row marker (schema). Doctrine: politeness gate may DELAY/SKIP a probe, NEVER discard a candidate — deterministic oracle is sole reject gate; throttle must not bury a real bug. Per-host key = lowercased hostname only; rps always clamped to ceiling. | Phase 1 | Recon politeness audit-defensible; fail-open so rate-limiting never costs recall |
| 2026-05-31: APPLY 01-08 (one-line installer). All 3 tasks PASS; 10/10 ACs; offline harness 26/26 (C1–C9); e2e_verify rc=0 (43 critical). SHIPPED install.sh (preflight→ensure_docker→seed→keys→bring_up→next_steps; --no-up/--unattended/--install-docker; health-wait captures rc so set -e can't abort, fails NON-ZERO on timeout naming down svcs, never success on half-up; ensure_docker never implicit curl\|sh) + seed_env.sh (idempotent, fills ONLY empty infra secrets via atomic awk temp-rewrite not sed -i, chmod 600, preflight before write, BYOK untouched) + test_install_e2e.sh (stub docker/curl, isolated temp copy). .env.example +HATCHET_POSTGRES_PASSWORD + LF; .gitignore +.env.backup*; README one-line+status; DEPLOYMENT Coolify recipe. **CRITICAL QUALIFY DISCOVERY: repo's shipped helper scripts were CRLF + NON-FUNCTIONAL on Linux (set: pipefail rc=2); plan recon misread them (compressed Read masked CRLF).** LF-normalized 4 scripts — health_check.sh+e2e_verify.sh (unprotected) + generate_keys.sh+entrypoint.sh (BOTH boundary-protected → 2 explicit user-approved scoped exceptions via AskUserQuestion; pure line-ending, zero logic, bash -n clean). M2 compose-default neutralized by always-override (seed fills all 6 incl HATCHET_POSTGRES_PASSWORD≠`hatchet`). Real .env seeded in tree (gitignored, not committed). shellcheck absent→skipped (bash -n clean on all 3). DOCTRINE: QUALIFY caught what recon's compressed file-read hid — always exec a shipped script, never trust a Read's rendering of its line-endings; installers reusing shipped scripts must verify those scripts actually RUN on the target OS; CRLF in .env/.sh is a Linux deploy footgun (corrupts compose values + `set` lines). | Phase 1 | One-line installer live + verified; package boots clean (CRLF defect that would have broken every shipped script + container boot fixed). Open: repo-wide CRLF sweep (01-09); live gates (01-09) |
| 2026-05-31: Enterprise audit on 01-09 (live deploy-gate closure). Applied 3 must-have + 3 strongly-rec; deferred 3. Verdict: conditionally acceptable (was not-acceptable). Caught release-blockers: (M1) recon smoke could probe an UNAUTHORIZED host — "safe target" underspecified + scope-JWT egress (the product's core control) not asserted → new AC-7: abort if no valid in-scope JWT, target derived from JWT claims ONLY (never hardcoded/3rd-party), assert ZERO out-of-scope egress, fail loud. (M2) `playwright install chromium` + bbscope curl-download were UNPINNED + UNVERIFIED → non-reproducible + supply-chain hole in the worker that handles scope/secrets → pin playwright/chromium + bbscope version, verify bbscope sha256 before chmod +x (fail build on mismatch). (M3) smoke mutates live bs-postgres + handles HATCHET token + secret webhook URL → test-data isolation+cleanup+re-runnable; NEVER echo/commit token or webhook URL (redact scheme+host). Strongly-rec: (S1) assert chromium on the RUNNING worker container not just built image; (S2) prefer a LOCAL deliberately-vulnerable container target over an internet host (kills authorization ambiguity); (S3) CRLF sweep idempotent + `.gitattributes` staged before `git add --renormalize`. Deferred: Coolify live-validation (remote), multi-arch builds (Phase 3), Langfuse smoke traces (Phase 2/3). Doctrine: a live recon smoke MUST be scope-JWT-gated + abort without authorization (never default to a real host); never bake unpinned/unverified binaries+browsers into a security tool's image; live smokes isolate+clean test data + redact secrets. | Phase 1 | 01-09 audit-defensible: scope-gated recon, reproducible+verified image bake, no secret leak, no live-DB pollution |
| 2026-05-31: APPLY/UNIFY 01-09 (live deploy-gate closure). All 7 ACs PASS LIVE on local Docker. SHIPPED: Dockerfile.control-plane (bbscope-build stage golang:1.24-alpine `go install github.com/sw33tLie/bbscope/v2@2d3bae66eeaa9c75d16af99af58b96a0c76a14dc` — Go module-checksum DB / sum.golang.org transparency log is the M2 integrity gate, static CGO_ENABLED=0 binary; Playwright/chromium baked install-deps(root)+install(user), pinned by Playwright 1.58.0 in uv.lock) + Dockerfile.recon (`COPY mcp/` before control-plane source so workspace members oracle-mcp+politeness-mcp resolve; subfinder/httpx/katana from go toolchain stage) + .gitattributes NEW (`* text=auto eol=lf` + explicit LF first-party + `-text` crypto/binaries/keys) + scripts/live_smoke.sh NEW (scope-JWT-gated; passive|active stages; M1 target-from-JWT-only abort-on-invalid; M3 webhook redact scheme+host + cleanup() drops recon_assets tag 'live-smoke' on exit trap). SMOKE: AC-1 bbscope invocable + chromium 145.0.7632.6 on RUNNING worker; AC-2 chromium executed injected script in-container (XSS oracle live); AC-3 worker registered 0 errors; AC-4 webhook POST→204; AC-5 passive subfinder→50 subdomains *.spotify.com rps=2 JWT-derived; AC-6 CRLF=0; AC-7 scope-gated abort-on-invalid. DEVIATIONS: (1) recon image 1st build failed uv sync exit 2 (workspace-member source missing — IDENTICAL root cause to the control-plane fix) → COPY mcp/ added; (2) one transient proxy.golang.org TLS-handshake timeout on a recon rebuild — network not code, retry succeeded; (3) SKILL GAP /e2e not formally invoked (live_smoke.sh is the functional equivalent) — warn-only. Human-action checkpoint: operator regen'd HATCHET_CLIENT_TOKEN, Discord canary SCOPE_WEBHOOK_URL, named pd-spotify (*.spotify.com, 4h, rps=2 → keys/scope_smoke.jwt). DOCTRINE: bake security-tool binaries pinned-by-commit + checksum-DB-verified (bbscope) and browsers pinned-by-lockfile (chromium via Playwright) — never `latest`; live recon smoke MUST derive target from scope JWT only + abort without authorization; live smokes isolate+clean seeded rows + redact secret webhook URLs to scheme+host; uv-workspace runtime images MUST COPY the whole `mcp/` tree before the member source or `uv sync` exits 2. | Phase 1 | Phase-1 live paths (bbscope scope-poll, chromium XSS oracle, worker triggers, real webhook delivery, scope-gated passive recon) all proven on the local stack; images reproducible + integrity-verified; no secret leak, no live-DB pollution. Open: 01-10 public release; 6 pre-existing integration failures (SubagentSimulator model_id drift) out of scope. |
### Deferred Issues

| Issue | Origin | Effort | Revisit |
|-------|--------|--------|---------|
| ~~HackerOne structured_scopes changelog verification~~ **RESOLVED 2026-05-27** via primary source (api.hackerone.com changelog, in docs/bountystrike_v6_phase0-1_technical_brief.md). Only program-level WRITE removed (Apr 7 2026); READ endpoint CURRENT; NEW scope_exclusions endpoint (Apr 11 2026). Phase 1 W1.2 UNBLOCKED: implement structured_scopes READ + scope_exclusions READ + merge; skip program-level WRITE. research/02-routing-ev.md:66-76 migration spec has WRONG date ("April 16" vs actual April 7) + needs scope_exclusions added — fix in a future Phase 1 plan. | Init (v6 plan) | DONE | Closed — see brief |
| DeepSeek pricing correction + cost model rebuild | Init (v6 plan) | S | Phase 0, Day 1 |
| EV decay constants derivation (lambda=0.00065 / mu=0.00963) — regression-fit or hand-tuned? | Init (v6 plan) | M | Phase 0 |
| ~~**Repo-wide CRLF**: many `scripts/*.py`/`*.sh` beyond the 4 normalized in 01-08 still ship CRLF — Linux-runtime footgun. Need repo-wide LF sweep + `.gitattributes`.~~ **RESOLVED 2026-05-31 (01-09 Task 2):** `.gitattributes` added (`* text=auto eol=lf` + explicit LF first-party + `-text` binaries/keys); working-tree CRLF=0 after renormalize; recurrence prevented. | APPLY 01-08 (2026-05-31) | DONE | Closed — 01-09 AC-6 |

### Blockers/Concerns

| Blocker | Origin | Status |
|---------|--------|--------|
| ~~No container runtime in WSL distro~~ **RESOLVED 2026-05-29** — Docker enabled (`docker info` UP). Custom image built exit 0; all empirical ACs verified live. | APPLY 01-01 (2026-05-29) | CLOSED |

## Session Continuity

Last session: 2026-05-31
Stopped at: 02-02 LOOP CLOSED (PLAN→APPLY→UNIFY). Sandbox docker-driver substrate shipped: real DockerDriver.run() (create→gate-register→start-a→report→deregister→rm; fail-closed on unset/unreachable gate; --cap-drop=ALL; egress_blocked verdict map); bs5-egress-gate sidecar (deny-by-default iptables in DOCKER-USER, NET_ADMIN+docker.sock); bs5/sandbox-runtime:0.1.0 image; compose wired. AC-1..AC-6 PASS; 40 tests pass + ruff clean (re-verified vs disk at unify); human-verify live egress proof approved. 02-02-SUMMARY.md written (AC-results table). Phase 2 = 2/6 plans complete (02-01, 02-02). NOT phase-complete → no transition.
Next action: /paul:audit 02-03 (audit gate ENABLED) → /paul:apply 02-03. 02-03 PLAN CREATED + recon-grounded (see SHAPE above). Apply MUST re-verify contracts vs disk FIRST: enum range, exploit-agent.md tools allowlist semantics, .mcp.json sandbox env, _exploit_one env_extras, validator-agent.md clean. Resume file: .paul/phases/02-full-agent-fleet/02-03-PLAN.md.
PRIOR (01-10 / Phase 1, now closed):
Stopped at: PHASE 1 COMPLETE + Phase 1→2 transition executed. 01-10 audited→applied→unified. v0.1.0 PUBLIC release shipped: nilhemdot/bountystrike-community (release commit 3709c08, annotated tag v0.1.0=3bdf41f, GitHub Release published). Secret-scan gate CLEAN (machine exit 0). v0.1 Community Edition MVP milestone 100% complete.
PRIOR (01-09, now closed):
Next action: (1) COMMIT the uncommitted Phase-1 work — 01-08 installer + 01-09 image-bake/CRLF/smoke + .paul. Recent commits show 374f6a6 = 01-07; so 01-08 AND 01-09 are uncommitted. Suggested: `feat(phase-1): ship 01-08 one-line installer` then `feat(phase-1): ship 01-09 live deploy-gate closure (bbscope+chromium bake, CRLF sweep, scope-gated smokes)`. (2) /paul:plan 01-10 (release mechanics: version bump 0.1.0, README, git tag, GitHub release). DO NOT transition Phase 1→2 yet — 01-10 not done.
SKILL-AUDIT GAP (01-09): /e2e was SPECIAL-FLOWS-required for Task 3 but not formally invoked as a skill this session. live_smoke.sh executed the equivalent recon→scan→validate→report pipeline checks live (functionally satisfied) — gap is procedural, non-blocking. Review whether to formally run /e2e in 01-10 or accept the smoke-runner as the e2e surface.
01-09 RECON-IMAGE FIX (for future image work): Dockerfile.recon Layer B `uv sync --frozen --no-dev` needs the full `mcp/` tree COPY'd before control-plane source — workspace members oracle-mcp + politeness-mcp must have source present or uv exits 2 "Failed to determine installation plan". Same fix as control-plane. Now encoded in both Dockerfiles.
Resume file: .paul/phases/01-community-edition-mvp/01-09-SUMMARY.md
01-08 PLAN SHAPE:
- STANDARD, 3 tasks, autonomous=true, 6 ACs, NO required skills (SPECIAL-FLOWS AI-red-team + /e2e are 01-09/out-of-scope). depends_on=[].
- SCOPE (confirmed AskUserQuestion): install.sh primary (curl|bash, docker-compose, any Ubuntu/Hetzner host) + Coolify=docs recipe only; secret-gen=unattended auto-gen (copy .env.example→.env, openssl-fill empty infra secrets, BYOK left blank, NEVER overwrite existing); SPLIT installer→01-08, live-deploy+release→01-09.
- Task 1: scripts/install.sh (new) — ensure_docker (curl get.docker.com|sh only under --unattended, else fail-clean non-zero per AC-4) → seed_secrets (calls seed_env.sh) → ensure_keys (reuse generate_keys.sh as-is, idempotent) → bring_up (`docker compose -f infra/docker/docker-compose.yml --env-file .env up -d --build` + health_check.sh retry loop, skipped by --no-up) → print_next_steps (blank BYOK keys + gen_scope_jwt cmd + URLs). Flags --no-up/--unattended/--help. + README one-line section + fix stale "Phase 1 signed off 2026-05-01" status line.
- Task 2: scripts/seed_env.sh (new) — idempotent: cp .env.example→.env if absent; openssl-fill ONLY empty infra secrets (POSTGRES/REDIS/HATCHET_COOKIE/HATCHET_POSTGRES/LANGFUSE_SECRET = rand -base64 32; LANGFUSE_SALT = rand -hex 16); never touch BYOK; safe tmp-file rewrite NOT `sed -i` keyed on value (base64 has /+= ). + .env.example reconcile: ADD HATCHET_POSTGRES_PASSWORD= (compose needs it, currently absent) + any other compose ${VAR}-no-default secret. AC-5 parity.
- Task 3: scripts/test_install_e2e.sh (new) — pure-bash, stub `docker` on PATH (records argv, exits 0), isolated temp copy (never touch real .env/keys). C1 fresh provision, C2 idempotent byte-identical, C3 stub records `compose up -d --build`, C4 docker-absent fails clean no curl|sh, C5 .env.example⊇compose-required parity. + docs/DEPLOYMENT.md "Deploy via Coolify" recipe subsection.
- BOUNDARIES: DON'T edit docker-compose.yml topology / Dockerfile.* (bbscope+chromium bake = 01-09) / entrypoint.sh / generate_keys.sh / init_wizard.py (keep interactive) / gen_scope_jwt.py / 01_schema.sql / oracle·validation·recon·scope code. NO live deploy, NO parked-gate smokes, NO image bake, NO git tag/GitHub release/version bump (all → 01-09), NO new Python deps, Coolify=docs only.
- RECON FACTS: generate_keys.sh already idempotent (skip-if-exists, --force, chmod 600) → reuse. health_check.sh = live service health; e2e_verify.sh = offline package-readiness. .env.example HAS POSTGRES/REDIS/HATCHET_COOKIE/LANGFUSE_SECRET/LANGFUSE_SALT but NOT HATCHET_POSTGRES_PASSWORD (the drift). Compose = 681 lines, caddy TLS, all 15 MCP incl mcp-politeness+redis. version=0.1.0 (bump deferred to 01-09).
PRIOR (01-07):
AUDIT FIXES (override plan shape below): AC-5 is now FAIL-OPEN — granted=False → probe returns TRUE (keep candidate, oracle is sole gate) + probe_skipped WARNING (NOT drop). host key = `(urlparse(url).hostname or "_nohost").lower()` never url. rps=min(rps_for_host,MAX_RPS_CEILING). wrap acquire/report fail-open. +AC-7 (clamp). Test AC-5: probe→True + GET not called + warning.
COMMIT STATUS: working tree holds 01-06 changes (8 files) + .paul + prior uncommitted 01-04/01-05 changes. HEAD still d588a5c (01-05 commit). Phase transition (after 01-08) will commit the phase; consider an interim commit before APPLY 01-07.
Resume file: .paul/phases/01-community-edition-mvp/01-07-PLAN.md
01-07 PLAN SHAPE:
- STANDARD, 3 tasks, autonomous=true. Scope="token-bucket only" + integration="in-process import" (both confirmed via AskUserQuestion). 6 ACs.
- KEY RECON (verified vs source): domains/recon/ is FULLY BUILT v5 carryover + tested (test_recon.py 21K, test_recon_main.py, integration). ReconService.run = subfinder→httpx→katana; tool_runner binary wrappers; persistence scan_jobs FSM; scope_filter w/ rps_for_host (DEAD CODE, unused); probers HttpxReflectionProber + _check_reflection; __main__ container entrypoint. orchestrator _phase_recon launches recon-agent SUBPROCESS (claude SDK), NOT Hatchet.
- mcp/politeness-mcp TokenBucketLimiter = BUILT + tested (test_bucket.py), per-host bucket + 429/503 adaptive backoff, acquire/report/status, MAX_RPS_CEILING=50, MAX_WAIT=30s, injectable time_source/sleep_fn. Standalone MCP; control-plane does NOT depend on it. → WIRE not build (same as 01-06 oracles).
- GAP = exactly the title: recon egress bypasses the bucket. Binary runs use global default_rps as -rate-limit flag (not per-host, no backoff). _check_reflection fires UNTHROTTLED in-process httpx GET per xss-candidate param (worst offender, no gate). rps_for_host + whole limiter unused.
- Task 1: root pyproject [tool.uv.sources] += politeness-mcp={workspace=true}; control-plane pyproject deps += "politeness-mcp"; extend HttpxReflectionProber w/ optional limiter + rps_for_host kwargs (default None=backward-compat). probe(): host=urlparse(url).hostname; acquire(host,rps_limit=rps_for_host(host)) BEFORE GET; granted=False→return False (skip, drop candidate); report(host,status_code) AFTER. acquire/report INSIDE prober (only place w/ status_code).
- Task 2: __main__.py builds limiter=TokenBucketLimiter(default_rps=float(scope_filter.default_rps)) per run + injects prober=HttpxReflectionProber(limiter=..., rps_for_host=scope_filter.rps_for_host). rps_for_host goes LIVE. structlog "recon.politeness.enabled". DON'T change ReconService order / tool_runner flags / pipeline.
- Task 3: NEW control-plane/tests/test_recon_politeness.py — real limiter + ManualClock + fake sleep_fn (mirror test_bucket.py), monkeypatch httpx GET. AC-2 acquire-before/report-after; AC-3 relaxed_hosts override 20 vs 5; AC-4 429→backoff_active; AC-5 wait>MAX_WAIT→granted False→GET NOT called→probe False; AC-6 limiter=None unchanged + test_recon*/green. Offline (no sockets/DB/docker).
- BOUNDARIES: DON'T edit bucket.py (frozen/tested), ReconService.run/ReflectionProber Protocol/scan_jobs FSM, tool_runner binaries, persistence/scope_filter logic, 01_schema.sql, oracle/validation. NO dnsx/naabu (Phase 2), NO binary-stage bucketing, NO Hatchet recon task, NO Redis backend, NO new deps beyond politeness-mcp.
- TRAPS: ruff-autofix strips unused imports BETWEEN edits (land import WITH its use — bit prior plans); `uv sync --all-packages` REQUIRED for workspace dep (01-04/06 trap); bucket single-process scope = correct for recon container; ASYNC ruff rule (httpx already async); probers.py kept import-light (don't drag asyncpg/__main__ graph in).
- NOTE: unlike 01-03/04/05/06, this plan does NOT touch tasks.py/worker.py/orchestrator.py — recon runs via subprocess, not a new Hatchet task. Files_modified=5, all recon/packaging/test.
01-06 PLAN SHAPE:
- COMPLEX, 3 tasks, autonomous=true, NO migration (findings/evidence schema already sufficient). In-process oracle import (NOT MCP stdio) — both confirmed via AskUserQuestion.
- KEY RECON: all 5 oracles (oracle_xss/ssrf/sqli/ssti/open_redirect) + idor/rce/ssrf_imds ALREADY built + field-validated TPR=1.0/FPR=0.0 in mcp/oracle-mcp (v5 carryover). DO NOT rebuild/modify oracle code. Plan WIRES them into Phase-1 runtime.
- control-plane does NOT yet depend on oracle-mcp → Task 1 adds it ([tool.uv.sources] workspace map already exists) + `uv sync --all-packages`.
- Task 1: new domains/validation/ (dispatch.py cwe→oracle map + services/verify_service.py VerifyFindingService). cwe map: CWE-79/xss→oracle_xss, CWE-89/sqli→oracle_sqli, CWE-918/ssrf→oracle_ssrf, CWE-74·94/ssti→oracle_ssti, CWE-601/open-redirect→oracle_open_redirect; strip "-candidate"; open_redirect needs target canary. Flow: claim (UPDATE…WHERE status='hypothesis' RETURNING) → dispatch → validated→record evidence (reuse EvidenceRecordingService.record → {content_hash_hex}) + status='validated'+evidence_hash; non-validated→rejected NO evidence; unknown cwe→validation_pending no-op; 0-row claim→benign noop.
- Task 2: tasks.py append verify-finding @hatchet.task + VerifyFindingInput(finding_id); worker.py register; orchestrator.py trigger_verify_finding + trigger-verify-finding subcommand (mirror trigger_record_evidence).
- Task 3: control-plane/tests/test_verify_finding.py (sqlite+local blob, MOCK oracles) — AC-1 validated→evidence+status; AC-2 rejected→NO evidence (moat); AC-3 dispatch table; AC-4 unsupported cwe noop; AC-5 already-claimed noop. + AC-8 XSS field-validation gate (run if docker, else document like 01-04 bbscope deferral).
- MOAT INVARIANT: status='validated' set ONLY after record() returns content_hash_hex.
- BOUNDARIES: don't touch oracle code/01_schema.sql/evidence service/existing tasks. NO dedup (Phase 2), NO exploit-chain hints, NO idor/rce/ssrf-imds dispatch, NO approval/report, NO MCP stdio.
- OVERLAP (benign): tasks.py/worker.py/orchestrator.py get append-only additions (same as 01-03/04/05).
- TRAPS: #2 Hatchet v1 @hatchet.task; #11 SciPy Welch (in oracle, handled); #14 Playwright (in oracle; worker needs `playwright install chromium` for live XSS → 01-08 deploy note; tests mock); ruff-autofix strips unused imports between edits (land import with use); `uv sync --all-packages` for workspace dep.
Resume context (prior):
- 01-05 LOOP CLOSED. Notification delivery shipped + verified (9/9 tests, migration 12 live-applied, ruff clean).
01-05 PLAN SHAPE:
- 3 tasks, standard, autonomous. Channel=generic webhook (SCOPE_WEBHOOK_URL, sends both Slack `text` + Discord `content` + events[]); trigger=inline in scope_poll; dedup=scope_changes.notified_at column.
- Task 1: migration 12_phase1_scope_notified.sql (ADD COLUMN IF NOT EXISTS notified_at TIMESTAMPTZ + partial idx WHERE notified_at IS NULL; NO CONCURRENTLY) + ScopeChange ORM notified_at. Additive; DO NOT touch 01_schema.sql.
- Task 2: notification_service.py deliver_pending(session) — select notified_at IS NULL, build Pydantic payload, httpx.AsyncClient POST (timeout 10, retry-once), mark notified_at=now() on 2xx only. FAIL-OPEN (no webhook→0; outage→0, no raise, no mark). Export from services/__init__ + domain __init__.
- Task 3: wire totals["notified"]=await deliver_pending(session) into scope_poll after ingest loop (same session block, in-handler import); new test_scope_notifications.py mock httpx (4 cases AC-1..AC-4; reuse existing scope test fixture).
- BOUNDARIES: don't change detect_scope_changes/scope_events/ingest_*_all/jwt_issuer/01_schema.sql/cron. Webhook only (no SMTP/UI/per-program routing/new deps).
- 01-04 OVERLAP (benign, acknowledged): tasks.py::scope_poll gets delivery call appended (append-only, no restructure).
Resume context:
- Phase 1: 3 of 8 plans done. Remaining: 01-04 (ingestion, PLANNED) → 01-05 notifications → 01-06 oracles → 01-07 recon → 01-08 installer.
- 01-04 KEY FINDING: scope_management is ~70% pre-built (v5 carryover). DO NOT rebuild arkadiyt/hackerone/jwt_issuer/ingest_service/scope_events — EXTEND. New files: integrations/bbscope.py, integrations/projectdiscovery.py. Modify: ingest_service.py (+ingest_bbscope_all/+ingest_projectdiscovery_all), workflows/tasks.py (+scope_poll @hatchet.task), worker.py (register), tests/test_scope_federation.py (new).
- 01-04 federation path: clients emit FederationProgram → _ingest_federation_program upserts → detect_scope_changes diffs (events come free). Mirror ingest_arkadiyt_all.
- 01-04 TRAPS: #3 bbscope v2 `poll`/`db` NOT v1 `h1 -t`; #8 projectdiscovery `dist/data.json` under `programs` key NOT chaos-bugbounty-list.json; #2 Hatchet v1 cron API (verify Context7); #15 jwt_issuer algorithms=["RS256"]; #10 Bugcrowd `_bugcrowd_session`. Intigriti --oos → CanonicalScope in_scope=False.
- 01-04 RECONCILE GATE: scope code predates 01-01 DB rebuild — run test_scope_ingest + test_scope_jwt vs live bs-postgres; fix code to schema (NOT migrations) if drift.
- COMMIT NOTE: 01-01/01-02/01-03 ALL committed (HEAD bacda9b). Working tree clean except untracked .token-savior-cache.json (tool cache — gitignore candidate, not part of plan).
- 01-03 SHIPPED: blob_store.py R2BlobStore._CHECKSUM_CONFIG (botocore Config when_required ×2); evidence_recording_service.py EvidenceRecordingService.record() (advisory-lock SELECT-guard idempotency, blob-PUT-before-commit, jsonb codec self-registered, genesis prev_hash b""); workflows/tasks.py record-evidence @hatchet.task + RecordEvidenceInput (raw_bytes_b64); worker.py registers [heartbeat, record_evidence]; orchestrator.py trigger-record-evidence subcommand (argparse --help). test_evidence_recording.py 4 tests. pyproject moto[server] + integration marker.
- LIVE STATE: bs-postgres + bs-hatchet + bs-hatchet-postgres UP + healthy. Worker STOPPED. bs-postgres volume bountystrike_postgres_data FRESH this session (init scripts re-ran; schema + 3 extensions verified). Hatchet token session-ephemeral at /tmp/bs_hatchet_token (Default tenant 707d0855-80ab-4e1f-a156-f1c4546cbf52) — regen runbook in 01-03-SUMMARY.
- KEY DOCTRINE this session: idempotency w/o UNIQUE = per-finding advisory-lock SELECT-guard; no jsonb codec existed anywhere (HashChainService passes dict→jsonb) → register on conn; aiobotocore needs ThreadedMotoServer not moto mock_aws; PostToolUse ruff-autofix strips unused imports BETWEEN edits (add import only once usage edit lands — bit me twice: botocore Config, argparse).
- UNCOMMITTED: working tree holds 01-03 changes (7 files) + .paul updates. 01-01+01-02 already committed (4c84845). Recommend commit before 01-04.
- HARNESS: NO corruption this session (model Opus 4.8 1M). Strict-sequential held throughout. Headroom (Pith) reported 120%+ near end.

---
*STATE.md — Updated after every significant action*
