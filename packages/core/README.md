# bountystrike-core — Apache-2.0

Shared, permissively-licensed primitives: schemas, MCP client, evidence model, shared libraries.

**License:** Apache-2.0 (see `./LICENSE`). Kept Apache-clean — **no imports from `community/` or `enterprise/`** so this tier can be consumed by any tier without copyleft propagation.
